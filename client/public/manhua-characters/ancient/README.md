# 古风原型设定板（示范图）

预期文件名：`{arch_id}_sheet.jpg`（例如 `arch_xianmen_sword_cold_sheet.jpg`）。

- 与都市 `char_*` 槽分离；路径由 `getAncientArchetypePreviewUrl` 解析。
- 请放入**无社媒 UI / 无外仓水印**的干净设计板（三视图+拆解清晰）。
- 缺图时画廊卡片会回退到渐变底 + 文案标签，prompt 仍以结构化字段为准。

扩展位（本轮未开槽）：战国将军、西域术士。
