改前证据表（0906）
最终结果：实验函数→GLM实际请求带冲突schema，返回及恢复必须逐来源验收。
范围：实验可选契约、共享网关显式strict开关、runner接线、目标回归；不改默认生产schema、读片参数、超时、账本、UI，不发付费调用、不部署、不合并。
入口：隔离整形脚本调用invokeNativeDeepReadGlmStructuring；生产6192/6391/6442等调用保持无契约行为。
生产者：buildBoundaryEvidenceBundle→deduplicateBoundaryEvidence→buildBoundaryCleanStructuringInput，四段七稿964条真实证据。
契约断点：runner 4532固定旧schema，bailianChat只在套餐Qwen分支发送schema；旧恢复205忽略schema差异。
存储：request/raw/parsed分别GCS；新契约参与恢复身份，新旧互不冒充；失败保留raw。
消费者：validateContent与恢复输出验收、实验provenance审计；不写产品数据库/云草稿。
副作用：保留原有鉴权计费fallback；strict请求不允许落到只支持json_object路径。没有新增账本调用或正式入口。
验证：实验模块、网关、runner、证据回读相关vitest一次批次；pnpm check；完整diff。
