# mvstudiopro Agent 硬规则

## 合并前独立子代理审查（2026-09-13 用户明确要求，全体 Agent 强制）

- 每次合并前必须安排至少一名子代理独立审查本次PR的真实完整diff、跨层调用链、测试与探针证据，不能只读PR描述或用主代理自审代替。
- 优先由未编写本批核心改动的子代理审查；审查记录写明PR号、被审HEAD SHA、发现项、验证证据和未验边界。没有可用独立子代理或审查未返回时不得合并。
- 发现问题先修复并交审查代理复验；所有阻断项闭合、无未解决问题才可合并。审查后HEAD改变须审查新增diff及受影响链路，不沿用旧SHA结论。
- 独立审查不替代探针验真、用户授权、紧邻合并的真实在途学习/部署查询，也不扩大付费、部署或删除权限；任何一道门禁未过均不合并。

## 最新逐段发布与有限付费授权（2026-09-13）

- 用户随后明确授权本轮开发验收所缺必要依赖直接安装，不再逐包询问。安装范围为本任务本地/隔离验收环境及相应构建配置，须核查真实缺口、同步版本/锁文件或系统包配置并执行运行验证；不借此更换技术栈、扩大付费调用或直接改装/重启生产机。生产更新仍经过独立审查、探针与发布门禁。

- 用户当轮最新要求：做完一段后验证、commit/push并开PR；合并前先做探针，无问题且紧邻确认没有在途学习与冲突部署才合并，核对后端与正式前台生效后再推进下一段。此节覆盖下方旧的跨批并行发布及历史PR编号指定，不把旧已合并PR当作当前交付目标。
- 当前批收口阶段②～⑤，⑥已经开始的独立离线原型保留但不纳入该PR；同批修正补原PR，不重复开张。开PR前仍实时查同分支、同主题与本人旧PR，遵守30分钟新建间隔。
- 本任务Mini已获有限授权：最多3次、每次5秒720p、总输出15秒，单次预算上限1美元、总预算3美元，不自动重试或切换模型。提交前展示完整提示词/参考/核价；原始及解析JSON、创建意图与任务ID永久保存，同ID查询。
- 付费候选为双人短打、四尾黑翼、人物表演；第三项必须有真实带骨素材验过，不用灰盒替代。预算不包含额外图片/3D生成，也不自动恢复此前延期的真实用户扣费退款验收；其他任务不得套用此额度。
- 基础设施启停、生产发布仍按各自授权，不能把模型预算当作任意远程操作许可。每十分钟写入知识库规则继续有效。

## 连续施工与发布授权（2026-09-07，所有接手 agent 必须遵守）

- 2026-09-07 晚间最新授权覆盖下述 #1412 指定：#1412 已合并，用户明确要求先开新的统一 PR，当前为 #1414。同性质或无冲突改动继续追加 #1414，不反复另开。用户正在学习，授权后先等半小时到一小时再查是否可合并；每次合并仍须实时确认无排队／运行学习和冲突部署，等待不影响继续开发。

- 漫剧工厂授权内仍有安全工作时持续施工，不得把等待 PR 合并、审查、部署或远端学习当作全部停工理由；独立批次继续开发、验证、提交、推送和开 PR。不得只回复“继续”却不执行。
- 用户已明确允许分批开 PR，不要求上一批先合并；同一批后续修正仍补原 PR，不重复开张。此条覆盖该任务旧的串行开 PR 门禁，不取消本地验证和审查要求。
- 最新交付指定：后续待部署改动统一追加 PR #1412，不再另开 PR；本条覆盖前述分批开新 PR 的默认做法。继续在该 PR 分支提交和推送；若 #1412 已关闭或合并，先查实际状态，不擅自另开替代。
- 同性质或互不冲突的改动集中同一张 PR，一起部署；新建 PR 至少相隔 30 分钟，不能按小补丁频繁开张。向现有 #1412 追加经验证的提交不受新建间隔限制，施工照常继续。
- 自行合并为条件授权：每次合并前必须重新读取远端排队/运行中的学习任务与冲突部署。只有确认无在途学习、无冲突部署才合并；有任务或无法确认时只暂缓合并/部署，其他施工不停。不沿用旧空闲结果，不取消用户任务。
- 用户已授权子代理并行赶工。分派独立有界任务，主代理同步推进核心施工；避免多 agent 覆盖同一文件。状态必须以真实产物、验证输出、提交和 PR 为据。
- 浏览器限定用户右侧已有漫剧工厂窗口/标签页；不得移动、关闭、另开替代或操作其他窗口。目标无法确认时停止该 UI 写操作，继续安全代码工作。
- 2026-09-08 用户要求：代理能读取的页面、弹窗、任务与产物状态自行核查，不再让用户代找弹窗或代查状态。工具超时不等于存在弹窗；先查实际页面与服务端对账，不重复付费提交。此要求不取消明确的新增付费范围、破坏性操作和安全权限门禁。
- 以上远程动作授权限于已授权漫剧工厂任务，不扩展到无关任务、付费重试、删资产或改业务合同。用户后续收回或修改授权时以最新指令为准。
- 接手 agent 必须同时读 `.cursor/rules/ship-always.mdc` 的最新连续施工条款；不能以换 agent、上下文恢复或等待发布为由遗漏本节。

本仓库所有 agent 开工前必须读取 `.cursor/rules/*.mdc` 与 `CLAUDE.md`。其中 `.cursor/rules/product-guardrails-always.mdc` 的“生产凭证只留 Fly”是不可覆盖的硬红线：

- 生产 API 密钥、Cookie、云凭证和服务账号只保存在 Fly secrets / 服务端环境。
- 本机、浏览器、前端、CLI、探针、测试、交接档和对话不得承载或导出真实凭证。
- 本机工具只能调用已鉴权的 Fly API；真实上游调用由 Fly 服务端完成。
- 缺少 Fly 入口时停手或补服务端入口，禁止要求用户把生产密钥配置到本机。

其余开发、验证、Git 与产品规则以 `.cursor/rules/*.mdc` 为真源。

## 视频与模型测试 JSON 永久保留

- 任何视频生成、视频分析、模板学习、模型探针、供应商联调或线上实测产生的 JSON，均属于付费/质量证据，必须在解析、门禁、转换、聚合或消费之前先可靠持久化。
- 原始供应商响应与解析/规范化后的 JSON 必须分对象保存，并记录对象名、字节数、SHA-256、模型/路由回执与对应任务或分片身份；不得只保留下游抽样、摘要或被截断的数组。
- 临时视频、音频、转码文件或 GCS/Fly `/tmp` 清理不得连带删除上述 JSON。任务失败、门禁拒收、重试、用户中止和部署重启也不得删除已得到的 JSON。
- 未经用户明确点名批准删除具体对象，任何 agent、worker、reaper、清理脚本或生命周期规则均不得自动删除这些 JSON；确需清理时必须先列出对象和影响并询问用户。
- 验收必须对账原始 JSON 与解析后 JSON 的证据条数（镜头、字幕、音轨、事件等）。数量不一致、静默采样、固定条数截断或只保留前 N 条，一律视为阻断项。

## 原生读片契约冻结与音画覆盖（0906最新确认）

- MEDIUM、无thinkingBudget、65536输出上限及既有采样、路由、温度、重试、超时参数均以代码中的实际冻结契约为准；不得因上下文压缩、覆盖不足或推测擅自调整。
- 用户用两到三天实测确定的描述字数上限保留：观察/构图/运镜各80，站位/身体/肢体道具各70，回应/动作概括各60，微表情/视线呼吸/灯光各58，转场50，景别/机位各28。均为上限，不要求写满。
- 生成要求完整记录全部镜头与音轨。程序验收：镜头或音轨覆盖率低于90%即不合格；不得延长区间或重复计数制造覆盖。
- 用户已撤销重点镜18项全部非空、强迫不同镜头使用不同表达的要求。保留基础结构与既有五项约束，描述按实际内容填写，不增加“看不清就解释”等填充要求。
- 旧门禁大部分保留，不另建复杂的生产验收体系。现行改动集中在读片JSON品质、音画缺口反馈、整形品质；探针对照统计不得擅自变成新的生产拒收条件。
- 温度沿用0.7→0.65→0.6，通过即停，门禁失败等待60秒再降档，三档失败后按音画覆盖、结构问题和可用证据零调用选出最佳原稿进入整形，选择不等于过门禁；三稿都没有可解析的非空镜头观察才停止。资源拥堵沿用30秒、最多4次同温重试。
- 原始与解析JSON逐发永久保存。新响应、缓存、同源恢复、canonical写回及整形后均执行适用门禁；历史请求身份保留，不能因取消额外内容强迫就重复购买旧证据。
- 修改冻结prompt/schema及验收内容必须有用户当轮明确授权，并同时验证未授权的配置保持不变；不得只改SHA或测试让变更过关。
- 本轮用户已授权改写后用同一份来源证据对照OpenRouter GLM-5.3与北京Token Plan Qwen3.8-Max。两路固定通道独立验收，失败不以其他路由冒充成功；不因此修改正式产品fallback链。实际输入、参数、次数及预算在调用前列明。
- 导出模板允许用户手动选择；既有本机下载方式、内容、鉴权与读片模型参数保持不变。

## 每十分钟写入知识库（2026-09-13 用户明确要求，全体 Agent 强制）

- 持续任务每十分钟必须将实际进度写入项目知识库；不是只读取、复核、口头汇报或等收班才记录。以墙钟计时，长测试/渲染期间也记录真实运行状态。
- 每条写明北京时间、负责 Agent、阶段、实际修改文件/提交、已执行验证及原始结果、产物位置、失败/未验证边界和下一步；无变化也简短记录真实断点，禁止编造进展。
- 主代理负责总进度，子代理每十分钟写入知识库内各自独立条目并向主代理报告，避免并发覆盖总档；派工必须附本规则和知识库绝对路径。短于十分钟的任务在交回前落档。
- 接手或压缩上下文后先读知识库总索引、当前任务条目及最新记录；与压缩上下文冲突时优先用知识库补齐，不沿用旧摘要。
- 当前分支、PR、任务、部署等可变事实仍须实时核验；发现知识库过期，纠正当前结论并保留带时间与版本的历史证据。
- 记录不扩大提交、发布、付费、删除或凭证权限。无法写入时报告准确错误并在已授权位置保存待补记录，恢复后补齐，不得声称已落档。

本轮主记录：`/Users/tangenjie/Downloads/mvstudiopro-知识库/漫剧工厂/0913-六阶段施工真实进度.md`。细则见 `.cursor/knowledge/kb/knowledge-refresh-0912.md`。
