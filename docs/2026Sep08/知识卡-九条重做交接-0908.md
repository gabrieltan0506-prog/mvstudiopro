# 知识卡九条重做交接 · 2026-09-08 17:40（Claude Fable 5.1）

**一句话状态**：回退 PR #1423（撤 #1420/#1421，未合并）+ 九条重做分支 `feat/knowledge-card-eight-0908`（基于回退分支，六个提交，本地全绿，**未推送、未开 PR、未部署、无付费调用**）。#1422 已关闭。

## 用户拍板（0908 下午）
1. 主体偏左/居中自选，横版 16:9 固定
2. 只出横版
3. 页数、文件大小、附图张数不设上限；加 EPUB 输入
4. EPUB 后台转 PDF 再交模型
5. 提炼档四改二：只留 GPT-5.6 Sol、Qwen3.8 Max；Sol→OpenAI 官方兜底，Qwen→百炼新加坡兜底；出图 gpt-image-2 EvoLink 优先、OpenAI 官方兜底；**Sol 只开 medium**
6. 超过 6 页不再降 2K
7. 进度百分比 + 成功/失败终态
8. 模型除读文字外参考原稿优秀版式：**不全渲染**，缩略目录页让模型挑「结构有特色」的页，只渲染选中页；成稿标〔参考原页〕，出图时作参考图重画
9. 精简版（页少）/ 高级版（页多）两档成稿

## 改动清单（feat 分支 vs 回退基线，21+ 文件）
- shared：`knowledgeCardDistillModels.ts`（两档 + 旧值迁移 + 旧档保价）、`knowledgeCardDistillSections.ts`（detailLevel）、`knowledgeCardSubjectPosition.ts`（新）、`knowledgeCardPagination.ts`（恒 high）
- server：`knowledgeCardDistill.ts`（通道链/兜底、目录页扫读 `makeKnowledgeCardPageSelector`、按页对齐分段 `buildPageAlignedChunks`、参考页标记指令、精简/高级）、`knowledgeCardDocumentPages.ts`（新：抽字/缩略目录页/选中页渲染上传/标记解析/参考页 URL）、`knowledgeCardEpubToPdf.ts`（新：jszip + Chromium）、`geminiPlatformCompositeTranslation.ts`（主体位置尾段、参考页指令、剥标记；旧视觉规范原样保留）、`proxyImageService.ts`（知识卡 providerOverride evolink、4K、参考页不走换脸指令）、`routers.ts`（有文件一律后台任务、base64 先落 GCS、subjectPosition、页数上限取消、参考页签名）、`jobs/runner.ts`（任务内抽文 + 分阶段百分比、带文件 120min）
- client：`KnowledgeCardProgress.tsx`（新）、`PlatformPage.tsx`（成稿/主体开关、.epub、进度条、去 2K 文案）
- 测试：新增 `knowledgeCardDistillGateway.test.ts`、`knowledgeCardDocumentPages.test.ts`（真实 Chromium 转 EPUB→poppler 逐页）、`knowledgeCardSubjectPosition.test.ts`、`knowledgeCardProgress.browser.test.ts`；改 4 个旧测试

## 验证（最后一次实际输出）
- `pnpm check --incremental false` exit 0；`pnpm exec vite build` exit 0（21.88s）；`git diff --check` 通过
- 12 文件 113 用例：111 通过后修两条（一条我的断言写错、一条 paidJobLedger 并行抖动），串行重跑 27/27 通过
- 子代理审查两轮（用户指定轮数）。一轮（P0 1、P1 5、P2 5）已全部修正：多 PDF 正文重复提炼（页费翻倍）、参考页 HEAD 签名必 403、多文件进度倒退、40min 墙钟、单请求参考页图无上限、上传百分比 ×0.01、EPUB 单引号资源、旧档位保价、扫读档位可配
- 二轮核实一轮修法全部成立，新抓 2 条 P1 已修：参考原页改按小节归属定位（分页按字数硬切时标记被切到邻页也不挂错图）；路由与出图共用 `resolveKnowledgeCardPageSource` 切片（含旧上/下篇兼容）。
- 三轮（用户指定）：一二轮修法全部核实成立；新抓 2 条 P1 已修：〔参考原页〕标记在分页/计费前剥离（不再多算页费）；标记规则只下发给带参考页图的段并按各文档 docKey 给示例（没图的段不给模型编标记的口子）。P2 未修（记录）：任务超时重排会重付扫读与提炼（页图幂等）；客户端 gcsUri 归属校验是旧洞；nav 进 spine 多一页目录。
- **违规自报（已修）**：初版沿用旧代码 ≤8MB base64 塞请求体、页图/目录页用 data URL 喂模型，违反媒体传输铁律；用户抓住后改为：文件不论大小前端直传 GCS 只带 gcsUri，上传图片/选中页/目录页全部落 GCS 用签名 https（4h）喂模型；旧 `extractPlatformDocumentText` 端点同步改 gcsUri。未打过 API，无损耗。EPUB→HTML 打印时图片内联 data URL 属本地 Chromium 渲染，不出网。
- 四轮：铁律核查无残留；新抓 2 条 P1 已修（上传路径已提炼的稿点「生成」不再二次提炼二次收费、不丢〔参考原页〕；多文件上传百分比按累计折算不倒退）+ 5 条 P2 已修。百炼新加坡抓 GCS V4 签名链：用户实测过（探针直读 mp4 签名链可行），记为已知结论，不再验；OpenAI 官方出图兜底只有 1536x1024（EvoLink 才 4K），日志已如实标注。
- 预览图：`知识卡-新控件与进度条-预览.png`（scratchpad，已发给用户）

## 未做 / 已知限制
- 线上未实跑：EPUB→PDF（Fly chromium）、目录页扫读、百炼新加坡 Qwen 多模态兜底、EvoLink 4K 参考图 edit 都只有离线测试
- 任务超时重排不复用已完成的扫读/提炼（页图上传幂等，LLM 调用会重烧）
- EPUB 各章 CSS 作用域全局（P2 未修）
- 模板画廊（12 张样张进产品）未做，用户已定「先不做」

## 接手顺序
合 #1423 → 推本分支开 PR（需用户回「发」）→ 合并后核 Fly 镜像 → 用户用原 PDF 亲手跑一次（付费）→ 出问题叫我查日志

## 0908 晚·线上探针与口径定案（Fly /tmp/probe-src 跑分支代码，账号 id=1）
- 第 1 轮（精简/压缩稿，出 2 页）：目录页扫读 3 发 → 选 37 页；8 段提炼 + 统稿 438s；出图 EvoLink 4K 各约 150s；两张成品达标（原稿的时辰表/分式图解/经络图被重画进卡）。发现：统稿把 79 节压到 60 节，用户判「比纯文字还少」；模型旁白（「第 17 页与前文冲突」「材料认为」）混进正文。
- 定案（用户逐条拍板）：**精华版**（原精简版）= 提炼主要重点；**高级版** = 主要+次要重点都包含、**不限页数**、能表格化/图表化的一律表格化压实（15 页上限只是打比方，已撤）；**单页硬顶 1200 字**（超过会乱码/字糊），分页按段落贪心贴上限不碎页；**出图多页并发 4 路**，奇页 EvoLink（4K）偶页 OpenAI 官方（1536×1024，按它规格不放大），另一家自动兜底；Sol 只开 medium；上传不论大小一律 GCS 直传。
- 第 4 轮（最终口径）发车时间见 /tmp/probe-out/probe.log；结果落 `2026Sep08/中醫養生篇高級版/`（每页 PNG + 提炼稿 + 合成 PDF）。第 2/3 轮中途按用户新口径停掉，未产生出图费用。
- flyctl 本机 config 认证会抽风报 no access token，用 `FLY_API_TOKEN=$(awk '/^access_token:/{print $2}' ~/.fly/config.yml)` 带 token 可用。

## 0908 深夜·最终探针结果（先图后文版）
- 精华版：27 节 / 3,964 字 → 5 页，提炼 359s，出图 4 路并发 200s；成品 `2026Sep08/中醫養生篇精華版/`（PNG×5 + PDF + 提炼稿）。
- 高级版：75 节 / 21,561 字 → 19 页，提炼 688s，出图 726s；成品 `2026Sep08/中醫養生篇高級版/`（PNG×19 + PDF + 提炼稿）。两轮通道链零兜底（EvoLink 与官方各自首发全部成功）。
- 本轮偶数页（OpenAI 官方）仍是 1536×1024，PDF 合成时等比放大补边；8e1b0d3 起官方显式 3840x2160，与 EvoLink 同规格。用户决定不重出。
- 教训：监听脚本用了 macOS bash 3 不支持的关联数组，两轮图落到同一目录，高级版前 5 页一度被精华版覆盖；按各自日志重拉纠正。写 shell 别用 `declare -A`。

## 0909 凌晨·合并事故与收口
- #1425 我叠在回退分支上，用户按我说的「先合 #1423 再合 #1425」操作，结果 #1425 进了回退分支而非 main（回退分支随 #1423 合并被删）。**是我的坑**：不该叠 PR。同批 19 提交 rebase 到 main 重开 **#1426**（对 main）。
- #1426 追加：Qwen 档主通道改百炼新加坡 token plan（EvoLink 兜底，档位 high；EvoLink 那支只认 low/medium/xhigh，high 映射 medium）；成稿档/主体位置改下拉；整套下载 PDF（服务端归一 3840×2160、逐页流式、落 GCS 签名链、弹窗被拦留链接）；分页不把 `##` 标题与「图：」行切开。
- 第五轮（合并前）审查 4 条必修已修，tsc/build/测试全绿。**等用户合 #1426**，合并部署后线上才有变化。
- 第六轮审查：P1 2（标题粘连造空页、测试空跑）+ P2 5，前两条与 P2-1/2/3/5 已修（c2d8324 之后一提交）。**已知未做**：导出 PDF 路由不校验成品图归属（成品对象名不含 userId，任何登录用户猜中对象名可为他人卡片换签名链）也无节流——另开单：出图落盘按 u{userId}/ 分目录 + 导出前校验 + 每用户每分钟限 2 次。
- 第七轮审查（远端 f9203c8）：P1 1（并发出图失败终态被在途页回填覆盖）+ P2 6，已全修（页序保留空洞、失败提示带已完成页数、分页指令去「信息密度高」、高级版要点/表格优先级、限流 Map 清理、过期注释）。子代理修的导出归属（f9203c8）：成品落盘 `generated/platform_knowledge_card/u{userId}/`，导出只放行本人前缀，历史成品明确拒绝提示逐张下载，每用户每分钟 2 次。
- **#1426 就绪待合**：七轮审查累计 P0 1 / P1 12 / P2 23 全部修正；tsc 0、build 0、知识卡测试全绿。合并部署后线上才有变化。
