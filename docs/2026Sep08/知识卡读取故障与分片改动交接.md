> 13:02 更新：524官方fallback已接回生产函数，复用现有官方凭证解析和同一模型；原raw/claim不覆盖。Gateway+Reading+Edition回归59/59，另resume/gateway28/28通过。真实进度已接DocumentPages→Reading→runner→session→UI，预渲染/阅读/规划分阶段计数；失败在恢复查询中同步显示。EPUB与80页上限修改已验证。分片规划已接自然大内容入口，但跨批带目标/预算仍明确拒绝，尚需后续闭合。第一轮全量类型报两处测试/并发编辑快照问题，已修，最终全量检查正在运行；前端构建已成功，最后少量UI调整后会再构建。没有线上付费实跑；提交/PR状态以本文后续更新为准。以下为12:56原始交接快照，保留历史断点供追溯。

# 知识卡读取失败与大文件处理交接

记录时间：2026-09-08 12:56（北京时间）。状态：部分实现、部分验证，尚未推送或发布。本文记录当下实际状态，不代表修复完成。

## 用户要求与截止

取消擅自添加的 EPUB 文件大小/解压总量限制；大内容由系统分片，不限制整套生成页数；沿用现有阶梯价，不另加成本门槛。新增真实阶段、完成量/总量、百分比、内容最后进展与后台心跳；不能把等待误报运行。用户在12:54左右要求十分钟内完成修复、推送和PR合并。最新要求先写此交接。

## 工作区与发布状态

- 实际工作区：`/tmp/mv-knowledge-card-4k-0908`（macOS可解析为 `/private/tmp/mv-knowledge-card-4k-0908`）。不要在任务显示的空目录施工。
- 分支：`fix/epub-chunked-reading-0908`，HEAD `f0efd6e6bf977c4a6d8820dc302612490fe5d369`。
- 该HEAD来自已合并的PR #1420；本批全部仍是未提交修改。
- 最新 `gh pr list --state open` 返回 `[]`。用户本轮已明确授权推送和PR合并；合并动作前仍必须实时核对所有在途学习任务及冲突部署。
- 上次正式发布：PR #1420，Fly和正式前端均已验证生效；本批未发布。
- 不得重新发起真实付费生产；本次调查没有提交新阅读/生图任务。

## 本次真实故障（已查原始回执）

任务：`kcr_18787cd6e4f59ed363c7806a0fd09f882ddcc995804cf9ce`。
文件：`《一本就能看懂中医-养生篇》275页-读书笔记-橘色的猫V1.3.pdf`。
模型：gpt-5.6-sol。

- 12:34:58.359 入队；12:40:14.869 为 failed，attempts=1。
- jobs output 仍是 starting、done=0、total=0，所以旧前端无法显示有意义进度。
- 12:38:02–12:38:06 已保存原页1–4的PNG；12:38:08.159 保存第一批 pages-1-4 的claim。
- 12:40:13.356 原始HTTP回执 **524**，HTML标题 `evolink.ai | 524: A timeout occurred`，Cloudflare指出 `api.evolink.ai` origin timed out。
- 12:40:14.712 raw.json永久保存，随后任务失败。
- 根因不是EPUB上限，也不是仍在读书：第一批4张图的同步阅读经过Cloudflare入口超时。PDF全页预渲染期间没有上报阶段/分母，失败文案又隐藏了HTTP状态。
- 524不能证明供应商没有在后台继续执行，不可直接重提旧批次或删除claim。

原回执相对当前用户阅读根路径：
`visual-reading-v1/00b39b1f414abb3d3326dadec24429f67d5e63755ce237c68c3ca1f2fb567901/d1-34f629c7391cfe04/pages-1-4/raw.json`。
源对象/凭证均留Fly/GCS，没有下载生产凭证或原始整本资料。

## 已实现与验证

1. EPUB五种总量拦截取消，改约2MB内部片目标，原子图片/表格行可以超过目标，不把目标当总书限制。完整spine顺序、正文、插图、有序列表续号、CRC/加密/路径校验保留。真实浏览器12/12通过，包括22MB原文件和90MB解压内容。
2. EPUB原文件及每片PDF存IndexedDB，以账号/源SHA/片SHA隔离；失败/刷新只补缺片，单片PDF、多片有序ZIP。浏览器6/6通过。
3. 真实React Panel大文件上传、第三片失败、刷新只续第三片并下载有效PDF ZIP，以及异步导入失败提示：2/2通过。
4. 正文导入回调改返回Promise，Panel await失败不假报成功。父页真实入口回归5/5通过。
5. 整套80页上限已从shared/Edition/router/UI撤除，保留最低4页和安全整数；代理报告shared27/27、Edition21/21通过，浏览器回归仍在收口。现有阶梯计价能按300页连续计费，无改价格。
6. 新独立进度组件已写，`progress={done,total,stage?,updatedAt?,heartbeatAt?}`，**尚未接入父页/服务端**，真实浏览器测试进行中。
7. 新 `knowledgeCardReadingChunkPlan.ts` 协调器10项单测通过，**尚未被生产调用**。自然大书无总量/页数上限，局部最低1页，全书至少4；多批带预算/目标页数分配仍有明确断点，不能称完整接通。

## 当前明确未完成

- Gateway仍对带图片请求使用api.evolink.ai，需核实并修正长请求通道。项目已有文字通道direct.evolink.ai；不能未经验真宣称带图也已成功。
- 原524任务要保留对账语义；修改代码不代表旧任务自动恢复。禁止删除raw/claim、自动重购。
- 进度生产：runner已有30秒心跳，但需分别记录内容进展时间和心跳时间；PDF预渲染必须上报真实页数及阶段，模型批次调用前也要更新；读取100%后规划阶段不能显示整个任务完成。
- 进度转换：readingSessionSchema目前只保存done/total；需保留新字段，父页onPoll绑定同一账号/会话，避免旧任务写进新账号。
- 页面失败：实际任务已failed但用户仍感到一直在读；要追踪恢复/轮询/UI异常显示，不只加进度条。
- 全量tsc第一次失败：epubToPdf.browser.test.ts第198–210行implicit any/unknown，已交原代理修复；尚未重新全量通过。
- 尚未跑本批完整构建、全部回归、最终diff审查；尚未commit/push/PR/merge/部署。
- 知识卡正文读取仍有200MB原件、64MB文本、1GB渲染图、单次500k证据等旧硬拦截；EPUB转换改完不等于下游全链无限容量。需内部批处理，不应直接替换另一个业务上限。

## 安全接手顺序

1. 阅读当前git diff和此报告，保留所有已生成证据/用户数据。
2. 优先修524通道、错误分类与进度生产→runner→session→UI；新读取不得覆盖旧回执，不得重提未知是否执行的请求。
3. 完成并验证大书规划接线，否则明确为部分实现，不能把独立新函数当成上线功能。
4. 执行类型/目标回归/构建；修正上述已知tsc失败；审查全部diff。
5. 用户已授权推送和合并。合并前在Fly进程里仅用服务端凭证读DB，核对queued/running学习任务及相关部署；不把凭证导出本机。
6. 发布后核验Fly实际版本/健康和正式前端bundle，不把合并当线上完成。

## 工具与命令

- `pnpm check --incremental false`：本批首次exit2（上述测试类型），待复跑。
- `pnpm exec vitest run client/src/lib/epubToPdf.browser.test.ts client/src/lib/epubPdfParts.browser.test.ts client/src/lib/knowledgeCardDocumentOrigin.parent.test.ts --minWorkers=1 --maxWorkers=3`：3文件23测试通过，44.23秒。
- 第一次只传maxWorkers=3导致min/max冲突、未跑测试；已用上条命令更正。
- Panel新增测试单独2/2通过；模块分片规划10/10代理结果日志 `/tmp/card-chunk-plan-tests.log`。
- Fly当前运行源码位于`/app/server`，无dist目录；调查使用`node --import tsx`。生产凭证只能在Fly运行时内读取，不得printenv或回传值。
- 浏览器固定用户原窗口，本轮没有操作、关闭或替换；本地无头浏览器只做离线测试。

## 当前修改清单（git原始输出）

```
 M client/src/components/platform/EpubToPdfPanel.tsx
 M client/src/components/platform/KnowledgeCardReadingPlans.tsx
 M client/src/lib/epubToPdf.browser.test.ts
 M client/src/lib/epubToPdf.ts
 M client/src/lib/knowledgeCardReadingPlans.browser.test.ts
 M client/src/pages/PlatformPage.tsx
 M server/routers.ts
 M server/services/knowledgeCardReadingEdition.ts
 M shared/knowledgeCardReadingPlan.test.ts
 M shared/knowledgeCardReadingPlan.ts
?? client/src/components/platform/KnowledgeCardReadingProgress.tsx
?? client/src/lib/epubPdfParts.browser.test.ts
?? client/src/lib/epubPdfParts.ts
?? client/src/lib/epubToPdfPanel.browser.test.ts
?? client/src/lib/knowledgeCardReadingProgress.browser.test.ts
?? server/services/knowledgeCardReadingChunkPlan.test.ts
?? server/services/knowledgeCardReadingChunkPlan.ts
```
