# 漫剧工厂第三批：同步主线与交付状态验真

日期：2026-09-06。状态：部分验证，尚未做第三批线上实跑。

## 本轮改前证据表

| 检查项 | 事实与范围 |
| --- | --- |
| 用户结果 | 沿用现有 Chrome 验收窗口；继续工厂 UI 和功能，真实动作和产物必须相符 |
| 发布边界 | PR #1392 已由用户合并；继续同一 #1384，不代合并，不触发付费生成 |
| 真实入口 | 成片坞与剪辑台交付要求区域、说明下载按钮、两个工程 ZIP 导出按钮 |
| 生产者 | `defaultManhuaDeliveryPackage` 提供默认要求，用户可编辑；不是媒体生产者 |
| 转换与存储 | `normalizeManhuaDeliveryPackage`、WriterSession、本机与云草稿；不改持久化字段名 |
| 消费者 | 两个 UI 摘要、Markdown 下载、工程 ZIP 内说明文件 |
| 副作用与恢复 | 说明下载只建本机 Blob；不调用模型/账本，不覆盖媒体。旧稿缺要求时复位本剧默认值 |
| 已知断点 | 默认值被计为“7/7 已就绪”；说明被叫作媒体交付包；旧稿缺字段会残留上一剧要求 |
| 相关回归 | 实际组件渲染、生产下载回调、真实 ZIP 解包、生产云恢复分支、混合草稿 roundtrip |

## 已核实的线上状态

- PR #1392：合并提交 `f0edf9fbb1e3dae31eace21e5ec9a5097425bffa`，上海时间 03:16:41 合并。
- Actions `33986627513`：正式前台检查 03:18:33 成功；Fly 部署 03:23:00 成功。本轮没有代合或手动部署。
- 现有 Chrome 单标签 `/canvas` 窗口刷新后仍有《墨菁传》与 6 张角色参考；没有点击较新云备份覆盖当前项目。
- 实际成片坞 0 集、可合成 0、待跑 0，却显示“交付包 7/7 项已就绪”。截图已在对话显示；截图受窗口遮挡右侧留白，不能当完整页面视觉验收。
- 缺片合成按钮禁用；未生成真实片段，未执行合成、烧字、付费模型或扣退验收。现有正文仍是 18 镜/90 秒骨架，不等同本机原稿已完整恢复。

## 本轮实际修改

1. 图片历史缓存增量 `cf1c987`：缓存按源 URL 身份保存；当前、旧版恢复核对来源，不再因复用节点槽位串图；本机/云图片历史不再裁成 8 条。模型参考图容量仍独立保留，不扩大模型输入。
2. 同步主线 `878b015`：保留双方知识记录；云草稿同时保留第三批造型/QC/视频历史与已合并的实际字幕回执。学习链变动仅来自主线，第三批没有修改冻结请求或锁测试。
3. `shared/manhuaDeliveryPackage.ts`：将摘要、文档明确为交付要求，不按默认填写量宣布媒体就绪。保留历史返回字段供兼容，计数不作验收依据。
4. `ManhuaDeliveryEditSection.tsx`、`ManhuaClipDock.tsx`：两个入口统一说明边界，按钮明确下载 Markdown；不自动配音/分轨/质检。
5. `manhuaProjectExport.ts`：ZIP 说明文件保留旧文件名兼容，README 明确其不是媒体或验收回执；删除隐式配乐承诺。真实媒体和失败清单原逻辑不变。
6. `OmniCanvas.tsx`：真实云恢复遇到没有交付要求的旧稿，恢复本剧默认要求，不残留上一剧配置。
7. 新增 `manhuaDeliveryTruth.test.ts`、`manhuaMergedDraftRoundtrip.test.ts`；更新 `manhuaSeriesMvp.acceptance.test.ts` 对原声合成及另行确认配乐的断言。

## 双向追链与邻接检查

- 正向：用户要求编辑 → 同源 state → WriterSession/云保存 → 恢复 → 成片坞摘要/说明下载/ZIP。缺配置恢复现在显式复位；已填写配置仍走原规范化，不清媒体。
- 反向：下载文件实际为 `text/markdown`，由真实按钮回调调用说明格式器产生；ZIP 解包确认说明存在但不增加成功媒体数。空项目仍拒绝工程包导出。
- 混合草稿：有效/失效造型状态 + 13 条视频 URL + QC 失败 + `reconcile_manual` 任务 + 两版成片独立字幕，在本机保存/加载、两次云往返及虚构签名器续签后均保留。字幕断言包括原文及 `1.125–9.875` 秒，不只检查字段存在。
- 两个工程导出按钮都消费同一说明格式器；无需另建入口。字幕开关与要求同步保留。配音、听障字幕、分轨等字段仅为要求记录，不冒充已实现媒体生产。
- 无新增扣费入口、重试、供应商路由或依赖；不能因此推断已有付费路径已通过线上验证。

## 测试、失败原因与修正

- 改前 4/4 失败：两个真实面板误报、文档标题误导、旧稿缺字段没有复位。修正后 4/4 通过，连同说明/会话/工程包共 27 项通过。
- 扩充 ZIP 用例初次 1 项失败：空项目被既有导出门禁拒绝。生产行为正确；改为先断言拒绝，再以一条真实文本产物验证说明不会计作视频，未放松门禁。最终说明渲染/处理器/ZIP/恢复 6/6 通过。
- 混合恢复 2/2 通过；夹具初稿使用非现行枚举，改为真实 `reconcile_manual` 后重新运行，无生产修改。
- 最终专项 12 文件、181 项通过（16.04 秒）。包含画布编排、布局、本机缓存、云恢复、交付说明、ZIP 与混合字幕合同。
- 首轮全仓：496 文件通过、3 失败、2 跳过；4679 项通过、3 失败、4 跳过（157.38 秒）。其中一项是工程说明改名后的旧文字断言，已更新为“保留原声”“配乐另行确认”并断言不再承诺“含配乐”；相关 17 项复验通过。
- 最终全仓：498 文件通过、2 失败、2 跳过；4682 项通过、2 失败、4 跳过（144.30 秒）。两项为主线已有读片 CLI 的 7201 上限断言；实际常量为 `240 * 60`，本分支相对主线相关学习代码与测试没有差异。未改冻结学习链或放宽测试。
- 最终 `pnpm check --incremental false --pretty false` 退出 0，包含新增组合测试；`pnpm exec vite build` 退出 0（30.80 秒），保留既有大包警告。完整 diff-check 退出 0，无新增依赖，未做 Docker 构建。

原始日志目录：`/Users/tangenjie/Downloads/2026Sep04/PR1392-release-NClAts`。本轮 `phase3-*`、`delivery-*` 日志均保留，包含红灯结果。

## 九层状态审计

| 层 | 状态 | 证据/限制 |
| --- | --- | --- |
| 需求边界 | 已验证 | 现有窗口、同一 PR、零新增付费，不代合 |
| 入口交互 | 部分完成 | 两个真实组件和下载回调离线验证；第三批未在线部署 |
| 数据生产 | 已验证 | 说明来源为要求，明确不冒充视频/字幕/音轨；缓存身份对应源 URL |
| 契约转换 | 已验证 | 混合本机/云往返保留造型、版本、QC、任务、实际字幕 |
| 服务副作用 | 部分完成 | 本轮无模型/账本改动，虚构签名边界；无真实收费/退款 |
| 存储恢复 | 部分完成 | 本机存储适配器/序列化回归通过；真实跨设备与生产存储未验 |
| 消费展示 | 部分完成 | 非空说明、ZIP 条目与真实 UI 渲染通过；真实影片质量未验 |
| 静态回归 | 部分完成 | 专项、类型与构建通过；最终全仓 4682 通过，保留两项主线旧失败 |
| 真实链路 | 部分完成 | #1392 线上空态已查；#1384 与真实片段、编辑、字幕、扣退全链未实跑 |

## 残余与回退

- 尚未完成同段有序变身三态、普通 ZIP 完整导入恢复、真实四引擎 10 秒质量、编辑三方质检、真实跨设备恢复、完整视频/SRT/音轨交付。
- 新查明的容量断点：解析器可读 29 镜/130 或 135 秒，但固定 6 段×3 静帧的后续重切仍可把它压成 18 镜、最多 90 秒；重切没有保留所有后镜独立运镜/表演字段。UI 和生产编排的分镜来源优先级也不一致。当前正在补真实编排复现，不把“尾句存在”当无损导入完成，不自动扩大生成次数或替用户重导项目。
- 过去已被覆盖或删除的图片缓存不能凭空恢复。仅有临时 URL 的历史记录不等于永久媒体仓库。
- 不回退到旧的 8 条截断和节点槽位缓存以掩盖错误。回退前先保留完整草稿与媒体，禁止删 JSON、任务、账本或历史产物。
- 交付说明口径可单独回退代码，但不应重新显示虚假的“7/7 已就绪”。用户保留合并及新增付费验收选择权。
