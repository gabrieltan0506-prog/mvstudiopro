# 0906 原生读片门禁历史实测

> 历史记录：下述“重点18项非空／零缺字段”已被用户后续明确撤销。当前改写以 `learn-coverage-0906.md` 和 AGENTS.md 最新要求为准；不再据此强迫补写内容。

## 当前追加授权：先改生成端，仍要求零缺字段

用户提出5镜容差后明确撤回，当前仍按缺字段重点镜0个验收。未启动过含5镜容差的付费探针。生成端继续要求重点18项完整；本次先优化重点窗口判定说明和真正送入重试的逐镜缺项反馈，内部验收容差不进入模型提示词。首发、重试、旧缓存、同源迁移、canonical写回与整形后检查保持同一零缺字段要求。新一轮仍使用同四片、Flash、Vertex、10fps与原三档参数，等待本地验证后启动。

状态：部分验证。代码与本地验证已执行；新三分支真实模型实测已结束：2片通过、2片未达字段要求，未部署，未验证正式界面到积分账单的完整生产流程。

## 改前证据与授权范围

用户要求重点镜18字段、简写镜5字段、原始画面与音轨覆盖至少90%、数值偏差最多10%，三档均失败停止。进一步明确授权将模型请求改成三种Schema分支，并授权同四段、Flash、Vertex、10fps真实探针。用户要求启动前说明、调用后立即报告；最新明确只看逐发镜数、重点镜、缺字段重点镜、覆盖率与音轨等数量，不再展示当前拒收原因，不依赖模型生成后自检；不得因个人喜好新增验收标准。

| 环节 | 实际入口与断点 |
|---|---|
| 用户入口 | 正式学习、重跑、仅重新整形进入 `manhuaNativeDeepReadExecution`；隔离选段实测进入 `runManhuaNativeDeepReadSelectedSegments` |
| 数据生产 | `buildGeminiNativeDeepReadSegmentRequest` 逐片传入模型、真实段界、fps；此前18/5字段只在description中，Schema共用required仅4项 |
| 转换与存储 | 原始响应、解析稿先永久保存，门禁之后写分片缓存；按来源、参数、分片身份核对，不删坏稿 |
| 最终消费者 | 门禁通过原稿进入整形、段卡、整集卡、音轨组装、导出；历史证据按现行内容要求重验 |
| 计费、权限、失败 | 正式产品权限和账单路径不变；模型已发生用量随失败抛出；本次隔离探针不写产品积分记录，不调用整形或选择器 |
| 回归 | Runner、Execution、PromptGroups、共享音轨测试；扩大原生链回归存在2个已在干净基线复现的CLI上限断言失败 |

## 实际修改与双向追链

### 15:45 普通镜详细输出放行：改前证据

| 项目 | 本次范围与证据 |
|---|---|
| 最终结果 | 用户明确撤销“普通镜写得详细、标成重点”拒收；只放宽该方向 |
| 入口与生产者 | 正式学习、选段探针、首发/重试由Runner读取模型shots；新响应有detailLevel |
| 转换与存储 | evaluate统一检查被缓存读取、同源迁移、canonical写回调用；不改原始内容与缓存指纹，不新增模型调用 |
| 消费者 | 同一raw进入提案、整形、音轨与导出；既有字段与内容门禁继续执行 |
| 权限、计费、恢复 | 不改入口权限与账单；不重写在途探针，保留原始证据；历史无标记兼容保留 |
| 检查 | 普通镜详细输出在新响应与缓存两路通过；实际重点缺细节、广告混类、缺标记仍检查；目标回归、类型、构建 |
| 当前断点 | 旧条件探针正在运行，本次门禁改动未部署，不把旧条件结果说成新改动实跑 |

1. 新出站 `shots.items.anyOf` 有 key/brief/ad 三分支。各自内容必填18/5/4项，另加必填 `detailLevel`，所以JSON实际required数为19/6/5。广告观察固定null，剧情观察不可null。所有新请求使用同一公开Schema构建函数；旧单结构只留给旧付费缓存指纹计算。
2. `evaluateNativeDeepReadSegmentAcceptance` 对新模型响应强制类型标记存在，按原始 `keyMoments ±6秒` 重新计算最低内容要求，检查非空与覆盖；普通镜额外写重点细节、标为key允许通过；首发及每次重试同路。类型标记不能替代程序计算。
3. 原始响应与解析JSON仍先保存再校验。旧两代请求身份可识别，旧稿可无新增标记，但仍按同一真实时间窗口检查18/5字段。整形Schema字节摘要 `c343b78c17e87ecd0679cac4fa93a9a9dee5f6a339d6e65fa4a48819a7ab7ed0` 未变，整形缓存身份保留。
4. 正向：实际用户/诊断入口 → 分片请求 → 原始/解析取证 → 程序门禁 → 合格缓存/下游。反向：下游原稿 → canonical缓存返回值复验 → 来源/分片/请求指纹 → 已保存原始响应及调用回执。另一按钮、旧缓存、只重整形、并发canonical返回仍执行现行完整性要求。
5. 音轨局部秒由每片 `endSec-startSec` 确定，最终加真实 `startSec`；没有固定281秒。用270/281/308/284秒直接调用生产函数，局部第10秒得到全片10/280/561/869秒。

修改文件：`server/services/manhuaNativeDeepReadRunner.ts`、对应Runner/Execution/PromptGroups测试、`shared/manhuaNativeAudioAnalysis.ts`及测试、`AGENTS.md`、brief、PROGRESS和本记录。没有新依赖、commit、push、PR或部署。

## 验证原始结果

- `pnpm exec vitest run server/services/manhuaNativeDeepReadRunner.test.ts server/services/manhuaNativeDeepReadPromptGroups.test.ts server/services/manhuaNativeDeepReadExecution.test.ts shared/manhuaNativeAudioAnalysis.test.ts`：4 files passed，361 tests passed；日志 `/tmp/branch-tests4.log`。
- `pnpm check`：exit 0；`/tmp/branch-typecheck2.log`。
- `pnpm build`：exit 0；`/tmp/branch-build.log`。
- `pnpm exec vitest run server/services/manhuaNativeDeepRead*.test.ts server/services/manhuaNativeAudio*.test.ts shared/manhuaNative*.test.ts`：1095 passed、2 failed；30 files passed、2 failed。两项均为未改动CLI测试期待7201秒非法；同样失败已在干净dc9e898基线复现。`/tmp/branch-regression.log`、`/tmp/gate-baseline-tests.log`。
- 标准JSON Schema本地校验：三个合法分支通过；30次逐字段删除和3次无效类型标记均拒绝。此项是本地结构校验，不冒充供应商验证。
- `git diff --check`：exit 0。三分支完整增量见 `/tmp/branches-only.diff`，已逐项审读。

## 九层状态

| 层 | 状态 | 证据与限制 |
|---|---|---|
| 需求与边界 | 已验证 | 用户授权严格门禁、三分支及本轮付费实测；禁止自加质量偏好和擅自换路由 |
| 入口与交互 | 部分完成 | 正式与诊断入口共享调用器，目标回归通过；未部署新版本，未从正式UI实跑 |
| 数据生产 | 部分完成 | 两轮8+10份真实原始/解析响应完整；新轮有63个缺字段重点镜，均选为brief分支，未解决生产质量 |
| 契约与转换 | 已验证 | 分支required、动态段界、类型重算、历史指纹及整形Schema不变测试通过 |
| 服务与副作用 | 部分完成 | 重试、无选择器、失败用量、永久取证测试通过；隔离实测不验证正式积分退款 |
| 存储与恢复 | 部分完成 | 两轮共18份原始与解析完全一致、SHA一致，永久对象完整；旧缓存恢复测试通过 |
| 消费与展示 | 部分完成 | 下游回归通过；未用新实测产物完成正式整形、页面与导出验收 |
| 静态与回归 | 部分完成 | 361目标通过、类型与构建通过；扩大回归2项干净基线已存在失败 |
| 真实链路 | 部分完成 | 独立付费探针已结束，真实字段问题仍存在；正式产品端到端及积分/退款未实跑 |

## 两轮真实实测

旧单结构Schema + 严格门禁：`strict-0906-h8-001`，15:29:53结束，8次返回，3片通过、1片三档失败。全部原始/解析逐份一致、SHA一致。每次画面与音轨区间覆盖100%；失败来自重点镜字段缺失。回执目录价折算合计约¥6.6087，并非实际用户积分账单。逐发数据已直接发给用户。

新三分支：`branches-0906-h8-002`，15:34:12启动进程，15:34:59–15:35:00四片首发，模型gemini-3.8-flash、Vertex、10fps；0.7→0.65→0.6，门禁失败等待60秒；资源拥堵按已有30秒/4次同温规则，禁止供应商切换。模型契约SHA `29eb843829bda71e4217e3aef406b76f963ce28949c706b9e869ff60d1714cd6`；上传程序SHA `41159f91a0e2470630ff4031e20c204f4bdc18c371c3174237246bbac40fe812`。15:49:39终态，共10次调用；第2、3片第2次通过，第1、4片三次后停止。回执目录价折算¥8.26922664。10份原始响应SHA全一致，解析JSON与原始候选正文完全一致，全部finishReason=STOP。以下指标是区间及字段检查，不是原片逐镜人工内容验收。


### 新三分支逐发核心数据

| 分片 | 尝试 | 温度 | 镜数 | 重点镜 | 缺字段重点镜 | 画面覆盖率 | 音轨覆盖率 |
|---|---|---|---|---|---|---|---|
| 1 | 1 | 0.7 | 85 | 23 | 13 | 100% | 100% |
| 1 | 2 | 0.65 | 99 | 29 | 12 | 100% | 100% |
| 1 | 3 | 0.6 | 72 | 30 | 8 | 100% | 100% |
| 2 | 1 | 0.7 | 41 | 24 | 11 | 56.94% | 100% |
| 2 | 2 | 0.65 | 50 | 21 | 0 | 100% | 100% |
| 3 | 1 | 0.7 | 37 | 15 | 6 | 100% | 100% |
| 3 | 2 | 0.65 | 36 | 12 | 0 | 100% | 100% |
| 4 | 1 | 0.7 | 31 | 16 | 5 | 67.03% | 100% |
| 4 | 2 | 0.65 | 48 | 27 | 6 | 86.74% | 100% |
| 4 | 3 | 0.6 | 41 | 14 | 2 | 100% | 100% |

三次覆盖缺口全部在尾段：第2片首次441–562秒（121秒）；第4片首次1030–1122秒（92秒）、第二次1085–1122秒（37秒）。63个缺字段重点镜全部detailLevel=brief、只有5项内容字段，均缺同样13项详细字段。不是解析丢失或MAX_TOKENS截断。直接断点是分支选择仍由模型判断：Schema能强制所选分支内部必填，不能保证模型选择满足跨keyMoments数组的时间交集要求。温度是否导致错选没有单变量证据。结构化生成与人工内容质量仍需分开验收。

### 普通镜多写放行：本地验证结果

仅Runner放宽expected=brief且实际标key的方向；新响应、缓存复验、同源迁移、canonical写回均通过同一evaluate入口，原文不变。请求prompt/schema和分片缓存指纹未改，未改变任何在途请求；本地验收摘要更新为c856f2494b7ce0e18e7f130902f87ba00ebd7bcfdeb3c7301402ed79371fa046。生产脚本仍是本轮冻结摘要29eb8438…，不能把本轮当作该放宽代码的线上实跑。

- 4文件目标回归：362 passed；`/tmp/allow-detailed-tests.log`。
- `pnpm check`、`pnpm build`：exit 0；`/tmp/allow-detailed-check.log`、`/tmp/allow-detailed-build.log`。
- 原样回放10份真实JSON到当前本地evaluate：2份通过，8份仍存在实际字段缺失，模型调用0；`/tmp/allow-detailed-final-replay.log`。
- `git diff --check`：exit 0。普通镜多写放行属于已实现并本地验证；未部署、未新增付费实跑。完整生产链仍为部分完成。


## 时间优先历史探针003终态

windows-0906-h8-003于2026-09-06 16:45:12结束，共10份模型返回，另有1次HTTP503未得到正文。第1/2片未过旧字段门，第3/4片通过；最终四片音画区间覆盖均100%。全部10份原始响应与解析JSON对账一致，finishReason均STOP；回执目录价折算约¥9.7381，并非用户积分账单。该结果不证明旧字段门能提升真实性，也不等于本轮覆盖改写已实跑。
