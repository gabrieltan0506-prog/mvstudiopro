# 学习、重新整形与 HTML 导出修复记录

状态：部分完成。代码与本地回归已通过当前检查；未合并部署，未完成用户浏览器的真实下载及入库验收。

## 改前证据与范围

| 层 | 证据与边界 |
| --- | --- |
| 最终结果 | 必填摘要有真实完整正文；重整形可换模型且不重新读视频；单集待审、批准、HTML 下载可用 |
| 入口 | 学习面板、实时任务、待审卡删除、放行重学、批准、导出 |
| 生产者 | 本次四份读片 JSON 两栏均有正文；旧 Qwen 整形后只剩“第”“国” |
| 契约 | 原先 schema 的单字符 pattern 与 minLength 1 未拦单字；标题 maxLength 未在消费者检查 |
| 存储 | 旧正式卡缺两栏；删除 proposal 不删除 approved；同请求快照误判完整新批次为旧结果 |
| 消费 | HTML 可显示单字；缺五维分析会用标签套句掩盖；广告区间在报告合并时丢失 |
| 权限/费用 | owner 入口；复用已保存证据，模型回执与原始/解析 JSON 永久保留；不自动批准或合并 |

用户最新决定：默认 GLM 5.3；可停止后双向切换 Qwen 3.8 Max/GLM 5.3，仅重新整形指定集；取消额外系列输出，自动及本次拟新增的手动系列入口均不提供。旧系列记录保留。原生读片冻结 prompt/schema/模型参数不改。

## 改动与双向追链

- 两必填栏统一拒绝空白、单字及旧占位文本。只有精确同源且全部分片具备正文才自动恢复，缺片拒绝；parser/增量合并取消这两栏600字静默截断。
- 提案写入与正式批准共用完整新批次判断：来源一致、批次不同、时间更新、全分片证据齐全；请求快照相同不等于输出相同。旧正式版归档，完整重学不混合旧镜头；approved 已提交后 proposal 同步失败可幂等重试。
- 原生逐集“删除此卡”和“放行重学”共用生命周期锁。两份原字节先归档、回读一致后 CAS 删除 approved/proposal；支持部分失败重试、仅正式卡存在。分片与模型证据不删除。
- 仅重新整形参数贯穿 UI、API、任务、计划、执行器和 runner。绑定原任务、用户、来源、集号和读片参数；等待旧 worker 退出，写入前检查取消。缺少完整可信 JSON 明确失败，不走新读片模型。
- 新模型分析校验拒绝超长标题、错误五维对象和有证据却缺分析的新格式；历史无标题格式兼容。报告缺五维分析时展示真实完整标签并明确标识，不再套句伪装。
- 广告账目由原始 non_story_ad 和源显式区间确定性组装；源 JSON 不变，不采信模型虚构区间。
- 自动系列聚合取消。保留逐集整理与待审，不额外产生系列模板。

反向核对：下载文件单字→待审卡单字→整形 parsed 单字→四份来源原稿非空，身份链以系列109554f0f515-g38f、第1集及来源摘要闭合；Qwen/GLM同源对照通过永久 request/raw/parsed 对象关联模型和费用。

## 真实探针对照

用户授权两路各一次，Fly 内执行；原始和解析 JSON、请求、用量和 HTML 永久保存。probe：summary-ab-0906-1788664790。

| 项目 | Qwen 3.8 Max | GLM 5.3 |
| --- | --- | --- |
| 耗时，含报告 | 725942ms | 379479ms |
| 必填摘要字数 | 271/266 | 271/280 |
| 是否触发原稿补栏 | 否 | 否 |
| 剧情镜头 | 298，原字段仅两处去句号 | 298，原字段全文一致 |
| 字幕/重点时刻 | 46/29 | 46/29 |
| 音轨块/段/cues | 4/15/32，全部同源一致 | 4/15/32，全部同源一致 |
| 分析问题 | 标题564字、五维分析缺失 | 标题18字、五维分析完整 |

原始299条镜头包含950.5–951.1秒广告；两路均保留对应排除账目，因此不是丢剧情镜头。供应商如何处理旧pattern仍是推断，不宣称已证实。此次不是旧pattern与新pattern的受控A/B。

HTML 为学习展示报告，按既有规则精选29画面、展示重点时刻±2秒内45/46字幕；完整 JSON 仍保留46字幕及所有镜头。不得称 HTML 是全部 JSON 字段逐项无损镜像。两路两摘要四段内容都进入报告。旧探针 HTML 的广告计数0已定位并修代码；新探针第一轮 GLM 已验证广告计数为1。

## 验收边界

已有目标测试覆盖空/单字、同源恢复、完整新批次、归档删除、模型双向切换、缺证据拒绝、分析错位和广告区间。本地执行两组相关 vitest：11文件294项、5文件458项全部通过；pnpm check、pnpm build、pnpm exec vite build 均退出0。前端构建保留现有大包警告。

未执行：新版本线上用户入口的切换/取消、真实批准、浏览器保存 HTML。当前 Fly 探针不是完整浏览器链路，不作为全链验收，不销毁临时探针。合并由用户操作。

## 新版真实验收进度

probe `learn-final-0906-1788670749`：GLM OpenRouter两次、Qwen北京一次。第一轮GLM通过：228/129字、标题17字、298镜头、46字幕、4音轨块、29重点时刻、1广告区间；HTML 6577871字节。重点时刻5处仅末尾句号变化，完整差异随回执保留。其余结果待回执，不预报成功。用户追加授权新加坡Qwen并发一次：`learn-sg-0906-1788671755`。所有请求均在Fly服务端执行，不复制生产凭证。
