# 0906 音画覆盖改写与两路整形对照

状态：部分验证。目标测试、类型检查与构建通过；扩大回归有2项CLI边界失败。本轮004付费读片探针已启动，结果待回收；未部署。

## 改前证据表

| 检查层 | 当前证据与施工范围 |
|---|---|
| 最终结果 | 完整记录音画；镜头或音轨覆盖率低于90%即不合格；撤掉重点镜18项描述全部非空及强迫表达不同的要求 |
| 允许修改 | 本轮用户明确授权的读片prompt/schema、字段门禁、音画缺口反馈、相应测试与说明 |
| 冻结保留 | 原生读片MEDIUM、65536输出上限、字数上限、既有模型/帧率/温度/重试/超时；不更换正式整形fallback链；用户后续已授权推送并创建PR，未授权合并或部署 |
| 真实入口 | Execution普通学习、重新学习、只重整形→Runner；独立诊断入口同用请求构建和门禁 |
| 生产者 | buildGeminiNativeDeepReadSegmentRequest→模型原始响应；默认与重试请求共用公开prompt/schema构建器 |
| 转换和存储 | 原始响应及解析JSON先永久保存，再执行门禁、缓存写回；旧请求指纹需要继续识别 |
| 消费者 | 段卡、GLM/Qwen整形、整集验收、音轨组装、模板入库及HTML导出；可选观察字段原有schema允许缺省 |
| 失败与计费 | 延用已有三温档与资源重试；不新增读片自动重试；旧证据不得删除或因新请求指纹误判而重复购买 |
| 已知断点 | assertRawShotFieldPresence在分片/缓存/整集均强迫重点字段非空；当前反馈只列画面缺口和重点缺字段，未对等列音轨缺口；描述重复禁令过宽 |
| 验证 | 目标Vitest、类型检查、构建、完整diff及调用点复查；本轮真实模型/最终UI/积分账单尚未验证 |

## 基线

改写前读片冻结摘要：f94b2b0129a26895f75fbb6094994b2ea09e02ee2dee27fed637fb90bb52fe85。
样例分片请求指纹：f3872c24c83082deae199217143260d47944cb496224f89afa60af382b666ea3。
整形Schema摘要：c343b78c17e87ecd0679cac4fa93a9a9dee5f6a339d6e65fa4a48819a7ab7ed0。
已有工作区改动先完整保留于本机施工前快照；本次不得混入其他任务或远程动作。

## 探针计划（待代码验证后形成实际运行清单）

同一份本轮读片原始证据分别送OpenRouter GLM-5.3和北京Token Plan Qwen3.8-Max。对照任务固定路由，单路失败单独记失败，不用fallback替代该路结果；正式产品链序不因此改变。两路共用内容与覆盖验收，原始和解析JSON永久留存。最终需报告音画覆盖、证据留存、观察对应、总结非空、输出token、耗时与实际回执成本。程序验收与对照原片内容抽查分开，不将时间戳并集等同真实读懂。

## 本轮验证与完成状态审计

| 层级 | 状态与证据 |
|---|---|
| 需求与边界 | 已验证：保留MEDIUM、输出和字数上限；撤销18项全部非空，保留90%音画覆盖判定 |
| 入口与交互 | 部分完成：Execution普通学习/重新学习/重整形沿原Runner；本轮尚未线上操作，手动导出模板另续 |
| 数据生产 | 部分完成：请求构建测试证明新prompt/schema进入默认和重试请求；本轮模型真实输出待探针 |
| 契约与转换 | 已验证：字段缺省允许、旧四版指纹兼容、整形schema保持；目标测试通过 |
| 服务与副作用 | 部分完成：未改路由/重试/计费模型；门禁失败、缺口反馈测试通过；线上账单未实跑 |
| 存储与恢复 | 部分完成：原始先存、旧指纹和整形缓存兼容测试通过；生产恢复未实跑 |
| 消费与展示 | 部分完成：正向请求→分片→整形/音轨、反向消费→原始证据调用点核对；新真实内容及UI待验收 |
| 静态与回归 | 部分验证：目标382项通过；check/build通过；扩大回归1067通过、2失败，完整diff与diff --check检查 |
| 真实链路 | 未做线上实跑：本轮两路对照探针、最终UI与积分账单尚未验证 |

执行记录：
- `pnpm exec vitest run`（Runner、PromptGroups、Execution、Audio、Comparison、CacheCompatibility六文件）：382 passed，退出0。
- `pnpm check`：退出0。
- `pnpm build`：退出0。
- `pnpm exec vitest run server/services/manhuaNativeDeepRead*.test.ts shared/manhuaNativeDeepRead*.test.ts shared/manhuaNativeAudioAnalysis.test.ts`：1067 passed / 2 failed，30文件，退出1。
- 两项失败是BatchCli与ProbeCli对7201秒非法分片上限的断言，相关CLI实现和测试不在本轮修改范围内；不以目标测试通过替代全量通过。
- `git diff --check`：退出0。

对照统计工具仅供探针使用，未接入生产拒收门禁。当前不得宣称线上完成，也不合并或部署。已有003探针证据见严格门禁历史记录；不得当成本轮新prompt验收结果。

## 手动导出主题：改前证据

| 项目 | 证据与范围 |
|---|---|
| 结果 | 三个原生报告导出入口统一弹出主题选择，自动匹配加既有五款，选择后下载 |
| 入口 | PlatformPage分集摘要、正式卡抽屉、待审卡→同一export回调 |
| 生产与转换 | 用户主题ID→封闭枚举API→Render输入→nativeReportThemePresentation；只改变展示 |
| 存储与消费者 | 自动版保留原对象路径；手动主题带独立后缀，签名下载读取对应HTML，避免主题之间覆盖 |
| 权限/费用/恢复 | owner鉴权、原始证据完整性校验保留；零模型调用；3秒重试保留同一选择，8秒下载豁免保留 |
| 验证 | 枚举拒绝非法ID、手动优先、自动兼容、路由转交/对象命名与渲染回归；线上下载未实跑 |

## 待审卡同步：改前证据

用户要求学习分片完成后待审面板自动同步，合并后的真实UI由用户验收。本次仅改前端同步，不改学习/计费/模型链。现有生产链为Runner保存分段卡→jobs进度检查点→3秒任务轮询签名→listProposals轻量列表。断点是同ID旧getProposalDetail覆盖最新列表行，且详情未随updatedAt更新。修复应保留列表最新进度，随选中卡版本刷新其详情；列表失败仍按原轮询重试，保留2分钟轻量列表兜底，不恢复全量大卡轮询。


## 导出选择与待审同步：改后验收

- 已实现：三个导出入口统一选择自动/五款主题，首次与重试请求保留同一主题；封闭枚举、owner权限、精确证据身份、分段预览规则保持。自动导出对象名不变，手动主题使用独立后缀。
- 已实现：待审卡的最新列表进度优先于旧详情；选中卡版本（updatedAt/分片状态）变化时只失效当前卡详情缓存。仍由现有3秒jobs轮询中的分片检查点触发列表重取，失败不记签名并于下轮重试，2分钟轻量列表兜底保留。
- 双向追链已检查：三个导出按钮→选择→API枚举→Render→主题插画/配色→独立HTML→原签名下载；从HTML所选ID反向到本次选择闭合。待审展示→最新列表DTO→GCS提案→Runner分段落盘与jobs检查点闭合，重字段走同ID详情更新；不触碰生产者、计费、原始JSON及恢复数据。
- 静态验证：`pnpm check`、`pnpm build`最终均退出0。曾因新按钮未导入导致类型报错，已改原生button后重跑通过。
- 回归：同步/接线/主题/路由/渲染五文件175项通过，新增五款手动主题实际HTML渲染检查另跑42项全部通过。
- 真实链路状态：部分验证。用户明确选择合并后自行验证待审自动同步和导出下载，本轮不执行此两项线上探针；尚不能称线上修复完成。
- 无新增依赖、数据库字段、学习计费、生产凭证或模型参数变化。已检查完整增量diff与相关调用点。

## 追加：三档失败后选最佳原稿进入整形（改前证据）

用户最新要求覆盖此前“三档失败直接停止”：现有0.7/0.65/0.6三档都未过门禁时，从已保存的原稿选数据最好的一份交正式整形，探针不要求同步改写。正式代码已实现，当前为本地部分验证，线上状态见后续实测记录。

| 层级 | 证据与计划边界 |
|---|---|
| 结果 | 三档尝试耗尽后仍可继续整形；选择不等于门禁通过 |
| 入口 | 普通学习、批量学习、恢复、只重整形共用Runner；不能只修改独立探针 |
| 生产 | attemptSegment原始/解析稿已先落盘，但gate失败直接throw，retry循环没有可选结果集合 |
| 选择 | 不新增模型调用；按音画覆盖、可用证据与结构问题排序，只选一份原稿，不拼补或篡改其内容 |
| 契约与存储 | server拥有的选择记录需放缓存信封中，绑定来源及原稿摘要，随不可变证据保存；不可伪装成模型自报合格 |
| 恢复断点 | 初始缓存筛选、processSegment复验、canonical写回复验、同源迁移现有拒收都会再次挡住候选，必须同一状态贯通，避免再次购买视频 |
| 快照与消费 | 当前已有旧Qwen候选跳过中间快照的路径；新候选不得生成空内容待审卡，最终快照需使用真正整形产物，导出也要核对消费口径 |
| 最终验收 | 选中原稿保留拒因，整形后沿用观察、分析、留存率及mapper验收；选择记录保留原始拒因，不宣称分片门禁通过，不恢复历史已撤销的全面集级门禁；付费失败与存储失败不得伪装成可选成功 |
| 验证 | 选择排序、成功提前停止、三档耗尽→真实整形调用、缓存恢复不重读、同源迁移、失败保留费用/JSON、最终仍不合格不入库；原有参数冻结回归 |


## 三档候选：改后双向追链与验收

状态：**部分验证**。正式Runner已实现三档失败后选最佳原稿，尚未部署，未做这条新分支的线上真实按钮验收。当前探针沿用严格三档程序，用户允许探针不改选择规则。

| 层级 | 状态与证据 |
|---|---|
| 需求与边界 | 已验证：仅三档门禁失败后选稿，不增加选择模型或第四次读片；0.7/0.65/0.6、MEDIUM、65536及冻结prompt/schema未改 |
| 入口与交互 | 已验证（代码与回归）：普通/批量/只重整形共用Runner；候选期间不发空中间卡，已有合格部分快照保留 |
| 真实生产者 | 已验证（回归）：每发响应/解析先落盘；20/40/30秒三份证据实际选第2份送整形，读片3次、选择器0次 |
| 契约与转换 | 已验证：选择状态在服务器缓存信封，绑定sourceDigest与raw SHA；原稿内伪造同名标记无效；不要求额外18字段非空 |
| 服务与副作用 | 已验证（回归）：费用在门禁前累计，三次共300000输入token；证据写失败不重买；原稿没有实际可用镜头时不制造空稿 |
| 存储与恢复 | 已验证（回归）：当前缓存、永久证据和同源迁移保留选择；只重整形读片0次，历史用量不计入本次；canonical返回另一份时消费存储真值 |
| 最终消费与展示 | 已验证（回归）：音轨错误段号99经整形修为0，最终快照不重解析坏原稿；六段分两批整形，最终证据包括全部六段；导出精确读取最终消费JSON，hash/来源/段证据不符即失败 |
| 静态与回归 | 目标538项通过；存储生命周期另79项通过。更大范围1183通过、2失败（原有两项7201秒CLI断言）；Vite3550模块构建通过。最终命令结果随本次提交更新 |
| 真实链路 | 部分完成：读片与两路整形探针见下；正式新选稿分支、真实面板、批准/下载和实际积分/退款未线上实跑；不以单测冒充线上通过 |

正向：入口→Runner三档原稿→服务器选择信封→canonical缓存→真实整形→mapper→最终消费证据→Ingest私有provenance→owner导出API→HTML。
反向：HTML→structuredCardObjectName及内容SHA→同一集/来源/segmentEvidenceObjectNames→canonical选择原稿及raw摘要→三次已保存响应。原始JSON、未选稿、旧产物均保留；公开DTO使用白名单，不透出私有路径。

已修正相邻空值漏判：现有镜头留存率原先仅在输出大于0时检查，0镜会漏过；现在0镜按0%处理，沿原整形重试链失败，不伪装成成功。没有恢复旧全面集级复验；缺口原稿经选择送整形仍保留缺口风险，只有实际新增/恢复的证据才能改善内容，不能延长秒位虚构覆盖。

无新增依赖、没有变更计费规则或公共权限/路由。对候选关联的坏整形缓存保留JSON，不删除付费证据；最终消费JSON按内容寻址永久保存并回读对账。既有非候选坏缓存处理不在此次改写范围。

## 004同源实测（2026-09-06）

来源《聚宝仙盆之杂灵根才是真BOSS》第1集，1122秒，四段。读片8次，四段程序门禁全部通过；294镜、38重点时刻、68字幕、31音轨、69声音事件。8份原始与解析响应逐发永久保存，条数与摘要对账一致；目录价折算¥7.46228304，非实际积分或供应商最终账单。

本轮同一份294镜证据输入两路整形，输入prompt SHA-256=`cc3c80cd352df172bf8834c1fcae0784909150dd9875c6cea2d0c7bbd26cd1c2`；每路一次，固定temperature=0.8、maxTokens=131072，GLM high、Qwen thinkingBudget=32768，超时15/25分钟。JSON与请求/用量永久保存，不提供或导出生产凭证。

OpenRouter GLM-5.3已返回：593736ms，61884输入/81121输出/11738思考token，回执$0.44357，STOP；上述五类证据条数全部一致，观察、分析、摘要、整集程序验收全部通过。生成标题“杂役残盆点物成宝·金手指验证型”，五维判词非空，30条镜头解读。

质量限制：抽帧发现末段切镜合并与事件时间提前；GLM忠实保留原稿不等于修正这些读片误差。因此本轮内容质量不能仅凭覆盖100%与计数一致宣布达标。北京Qwen在824865ms返回STOP，59366输入/67571输出/11292思考token；套餐回执costUsd=0不能当作无成本。294镜/38重点/68字幕/31音轨/69事件全部保留，但缺少classificationProseZh，992.5秒重点从“情绪”改为“剧情”，craftReadZh为44条（GLM为30条），本轮整形验收未通过。两路均只调用一次，未追加重试或替换路由。


最终静态验收命令与原始结果：

- `pnpm check`：退出0（最终日志`/tmp/best-selection-delivery-check.log`）。中间曾报新增测试Buffer泛型不兼容与catch unknown，均修正后通过。
- `pnpm build --incremental false`：`tsc --incremental false`，退出0；避免增量缓存掩盖类型问题。
- `pnpm exec vite build`：`3550 modules transformed`，`built in 55.31s`，退出0；现有大chunk警告仍在。
- 目标9文件vitest：`Tests 538 passed (538)`，退出0（`/tmp/best-selection-final-tests.log`）。
- Store归档/退役/生命周期3文件：`79 passed`；随后新增最终消费证据批准重试回归，生命周期与消费证据2文件`70 passed`，均退出0。
- 扩大35文件vitest：`2 failed | 1183 passed (1185)`，退出1；仅原有BatchCli/ProbeCli对7201秒上限的断言失败，未修改这两个CLI。
- `git diff --check`：退出0。当前仓库没有独立lint命令；未新增依赖，不执行依赖升级。

修改范围：Runner/AttemptSelection、SegmentCache/Execution、StructuredCard、Ingest/sharedBank/Store、导出router/Render及对应测试；新增私有证据路径校验模块，更新AGENTS、PROGRESS和本报告。未改读片prompt/schema、模型参数、计费或权限规则。付费探针JSON在Fly/GCS永久保存，不加入Git；此处只记录验收结果。

残余风险与待验收：正式新选择分支尚未部署；真实UI同步/主题下载按用户安排在合并后自行验证。读片时间错位/切镜合并仍存在，Qwen本次分析缺项，不把本次实验宣布为整体质量达标。既有7201秒CLI断言仍红；未实跑正式积分扣费、退款与真实用户批准操作。


两路供应商原文回放对账（Fly零模型读取）：从已保存SSE重建完整正文，与永久parsed和本次消费raw逐字JSON对比，两路均matched=true。GLM原文对象26012888字节、parsed215065字节；Qwen原文5337407字节、parsed207990字节，全部SHA核对通过。Qwen缺五维判词及44条解读已存在于供应商原文，不是本地解析丢字段。完整对账记录已永久保存为004探针的structuring-roundtrip-audit.json。
