# 漫剧工厂：B 今日 / C 三日进度（内部）

分支：`feat/manhua-agent-loop-vimax` · PR 不合并，只推送备份与评审。

## B（今日完工）

- [x] 机位密码 10 角入库 + 分镜芯片
- [x] 一体参考板 UI（左栏 + Tab）
- [x] 粗剪轨排序 + 剪辑节点库
- [x] 机位写回反推「机位选定」表 → 静帧/成片注入
- [x] localStorage 持久化机位/粗剪序

验收：工作台可选机位、看参考板、排粗剪；刷新后机位仍在；出图 prompt 含机位句。

## C（三日内完工）

| 日 | 目标 | 交付 |
|----|------|------|
| **D1** ✅ | 剪辑阶段壳 | stepper「剪辑」+ `ManhuaEditMultitrackPanel`（V1/V2/A1/字幕占位）接粗剪序 |
| **D2** ✅ | 细剪 + 字幕入口 | 进出点 ±0.5s；字幕轨→cue/SRT（不烧字）；包装动效挂 motion 库（Logo/字幕/产品，最多 2） |
| **D3** ✅ | 质检返工闭环 + 导出 | 剪辑台嵌智能质检；未过/缺片一键返工；可导出镜勾进成片坞；单测覆盖 |

不做：外接专业 NLE、云端协作时间线、真实设定集大图生图（二期）。

## C 验收（剪辑阶段）

1. 步进可进「剪辑」；多轨随粗剪序 + 细剪进出点变化  
2. 字幕轨开关 → cue / 可复制 SRT；包装动效可选最多 2  
3. 质检条显示各维；未过可「重出成片/静帧」或「仍采用」；可勾选进成片坞合成
