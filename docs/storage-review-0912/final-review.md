# PR1451 / PR1452 合并前复审

用户明确授权两张原 PR 有问题直接修正后合并；不新增付费生产或删除。基线：1451=179aed1c，1452=d1347f86。

| 层次 | 证据和状态 |
|---|---|
| 需求与边界 | 已核对原分支与实际部署；修正阻断后原 PR 合并；付费扣退仍延期 |
| 入口与交互 | 已验证：实际生产轮询闭包的在途唤醒、旧回包、前后台、卸载回归；BGM 本地 pending 保持5秒 |
| 数据生产 | 已验证代码与离线内容：原公开写入生产者转公共GCS；流式上传传120秒取消信号；真实生成未新增调用 |
| 契约与转换 | 已验证：新客户端显式 compact DTO，旧客户端完整响应；详情与下载仍完整；固定namespace拒绝任意签名 |
| 服务与副作用 | 已验证离线：上传失败终止；Blob仅旧公开读、30秒超时，两store明确缺失才410，未知错误不冒充缺失；删除按attempted限流，target未知不删 |
| 存储与恢复 | 已验证离线：稳定URL逐次续签、旧Blob原字节读取/双store回退、草稿旧契约不变；真实保留37076字节图片在发布后读回 |
| 消费与展示 | 已追链：API输出→稳定URL→签名跳转→下载扩展名；反查写入生产者相同namespace。正式UI播放下载未实跑 |
| 静态与回归 | 17文件294测试、清理器16测试通过；Vite通过13.13秒、有既有大chunk警告；非增量类型和服务端构建修正Set旧目标兼容后均退出0，handler16项复跑通过 |
| 真实链路 | 部分验证：本轮Blender5.2.1新增边界后5/5、32.92秒，相关114项通过；正式登录UI、Fly3.4.1新构图及扣退未实跑 |

PR1452复现原tight脚网格最低y=0.0195865277侵入2%安全边，尚未越出实际画框。修正后该相机回退auto；8000组固定种子静态相机采样未检出安全边侵入，不能外推全部运动场景。

线上门禁以直接只读SQL查询全部queued/running、磁盘任务状态和GitHub部署为准。4月报告awaiting_review已有草稿，代码明确为等人工决策而非运行；不修改它。每次合并重新查询，不复用旧结果。

证据目录：/Users/tangenjie/Documents/Codex/2026-09-06/new-chat/pr1451-pr1452-final-merge-0912；Blender完整原始/解析JSON和场景在/private/tmp/mvs-final-1452-evidence。未运行真实清理器删除、未改计费/API/schema/依赖。生产凭证始终留Fly。
