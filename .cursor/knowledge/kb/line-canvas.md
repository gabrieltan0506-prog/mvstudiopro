# line-canvas · 漫剧工厂线（动态层，每班收班更新）

> 更新：2026-08-12 深夜（Fable 5 接手班）。产品骨架见 `../manhua-factory-brief.md`，此处只放当前态。

## 红线（先背再动手）
- **0 号红线：视频/成片生成永远不得由 agent 触发**——最远推进到「审阅成片提示词」+报价单；紫钮（确认静帧生成全部成片）只能用户点。
- 每段恰 3 镜是全仓不变量；三阶段门控（剧本→资产→静帧→成片）；导入顺序**先剧本后资产 ZIP**（反了清资产）。
- 画布批次运行中，agent 自己不得合并部署（双烧 $1.1–1.7 实锤）。

## 雁门照山河 ep01 现状
- 设定图 67 张（人物/场景清零，道具剩灶牌特写 1 张未出）；22 对同名重复等**用户**点清（agent 写请求被 WAF 拦，点了无效）。
- 静帧：第 01 段已近齐（以页面「静帧 N/9」为准）；成片 **0**。
- 段 01 提示词七要素 5/7：对白四句缺失+秒轴只 1 拍——齐帧后点「审阅成片提示词」重铺应自愈；不自愈则修 `client/src/lib/canvasDramaStudio.ts` ~1947 dialogueLines → `shared/manhuaScriptWorkbench.ts` hydrateWorkbenchShotsWithSegmentDialogue 注入链。
- 出片报价：四段 2.5·720p ≈ $18–19，等用户出片令。

## 在修 / 待办（刀序）
1. [在修，分支 fix/canvas-zoom-and-node-size] 缩放控件（FreeformCanvas.tsx 底部 ±/复位 + Ctrl/捏合 wheel）+ 节点尺寸三组常量 1.5×（canvasDramaStudio.ts ~2429，间距必须同步放大否则互叠）。
2. ZIP 导入造 URL 入口（importAssetZipFromUrl 路由 / URL 粘贴框）。
3. 齐帧重铺审阅 → 四段七要素核（对白！）→ 报价单 → 等出片令。
4. 出片后：段 01 抽谢无咎声线 → 后续段 audio_urls 复用 → 成片坞拼 120s。
5. 认领按名回绑 cast id / 批次断线续航（抄 #1190 模式）/ H3 官方契约接入 / 模板 listApproved 服务端剥具名。

## 2026-08-26 · 后期交付现行口径
- 配乐间在成片坞内：剧情只先编译可编辑 brief；用户二次确认后才建立 Suno V5.5 异步任务。任务 ID 先落 jobs，再轮询原单；上游临时音频必须全部验真并转存本人 GCS，刷新/部署恢复不得重复 POST。
- 卡点表不是展示稿：真实画面事件与客观电平必须下沉为 `bgmSeekSec + volumeExpr`，进入 `bgm_mount` 的 ffmpeg 链；顺序固定为曲内裁切→片内延迟→逐帧音量→淡入淡出。
- 最终交付顺序：成片→可选 2K/4K 超分→贴 BGM→响度验收。超分结果独立保存，禁止覆盖原片；漫剧工厂与自由画布复用同一判据。
- TTS 当前生产入口只走 Token Plan：新加坡优先，只有明确 4xx 才换北京；网络、超时、5xx 或 2xx 下载中断均停止，禁止换区重烧。上游五字段固定为 `model/input/voice/response_format/seed`，情绪写入 input 标签，验声通过后才入本人 GCS。
- 配乐与 TTS 的公开计费未拍板，普通用户继续不可见；admin/supervisor 可做内部验收。未获当轮明文授权不得点击生成配乐、TTS、超分或视频按钮。


## 2026-09-01 · 读片链路未闭环

两次LOW已返回但内容不合格：漫剧30条全缺lightingZh，真人末条205—313秒长108秒。新本地候选只整理生成前长镜分段与30秒输入要求，程序33秒容差和默认MEDIUM保持；全仓3848通过/4跳过、类型与本地构建通过，未做新API或生产消费验收。缺字段放行、生产seriesKey40上限仍待修。当前接手真值与完整证据索引：`/Users/tangenjie/Downloads/2026Sep01/交接-0901凌晨-PR1328读片链路与未验证改动.md`。


02:03新增环境/道具观察候选：三字段prompt/schema共用通用说明及80/70/60字上限；新全仓回归、常规/探针类型、无增量build通过，未做新API。hint传递已存在但主入口尚无场景事实生产者，本轮未扩建粗读。逐字段和真实旧请求离线对照见交接目录的`环境道具观察/`。


## 2026-09-01 · PR1328逐镜动态观察（本地验证通过，模型待测）

- 用户接受同一发API内逐镜输出hintZh：按startSec/endSec/evidenceRole/hintZh顺序生成，story非空观察，广告null占位；与调用前的输入hintZh区分。新观察字段≤80字，未入画或无法辨认允许明确说明。
- 当前生产请求、当前缓存及同源迁移检查观察必填和时间顺序；历史JSON兼容读取。GLM整形后校对hint原文及来源时间覆盖，缺失或串镜停止消费，不自动重发GLM。映射、模板解析/优化、编剧消费、报告和UI摘要保留字段；旧付费原稿不改写。
- 用户最新探针参数：MEDIUM / 0.65 / 12fps / maxOutputTokens65536；旧LOW驱动会克隆旧请求，不可仅换参数重用，新探针须调用当前prompt/schema工厂。
- 本地验证：pnpm check、全仓Vitest（414文件通过/2跳过，3853项通过/4跳过）、无增量pnpm build、两份实际请求离线重建及旧原稿不变检查退出0。探针类型与Vite构建、镜像和真实新结果另记后续回执；本条不等于画面识别已修复。
- 完整证据、前置表与候选请求位于Downloads/2026Sep01/逐镜动态观察。内部33秒拒收线、旧lighting字段放行缺口与seriesKey40字符限制未修改。


## 2026-09-01 03:02 · PR1328逐镜观察三次单发实测

- 三次均为真实独立MP4首片、MEDIUM、12fps、maxOutputTokens65536，各发一次，无自动重试。漫剧是5片中的319.066667秒首片；真人是9均分片中的313.04秒首片，非整集加阅读区间。
- 漫剧0.65：obs_20260831183804_1b44baae，32剧情记录／1音轨段／5声音事件／7关键时刻／8字幕，逐镜18字段无缺项，最长26.5秒；用户明确确认本轮过关，不因字幕追加验收。
- 漫剧0.7：obs_20260831185310_2f5ea5ef，41剧情记录／3音轨段／5声音事件／9关键时刻／9字幕，逐镜18字段无缺项，最长18秒；HTTP200、STOP、程序结构通过。同素材请求仅temperature不同，单轮数量差异不代表稳定质量结论。
- 真人0.7：obs_20260831185310_24c10c7f，35记录（30剧情＋5广告）／1音轨段／4声音事件／5关键时刻／4字幕；逐镜字段完整，但原稿时间轴0..513超出313.04秒片长。13条记录、关键时刻343/457及声音事件320/434越界；原数字差有5条超过30秒（含1广告），程序因4条剧情记录拒收。全部镜头边界数字符合MMSS拼接模式，末尾513对应05:13；这是诊断线索，不得自动转换后冒充已验真或认定原片存在54秒真实长镜。真人0.65只做预检，没有发出模型请求。
- 原始响应与解析稿先分别永久保存，三份目录共18项manifest文件SHA、字节和原稿深等核验通过。完整比较：/Users/tangenjie/Downloads/2026Sep01/逐镜动态观察/温度对照-三列.md及同名JSON；完整交接：/Users/tangenjie/Downloads/2026Sep01/交接-0901凌晨-PR1328读片链路与未验证改动.md。
- 0.7复用镜像sha256:8000b9aa32e32d68ea63376b93b39c6e69abebc3611e682555ae243b8f2f4253，仅运行时探针驱动四处温度审计与两套配置变为0.7；生产TS源码未再变更，默认温度仍0.65。旧机器、原始分片与付费JSON保留。本轮未追加原帧／听音、GLM整形／模板UI全链实跑；未commit、push、合并或正式部署，不能称完整生产闭环。

## 2026-09-04 · 导演策略与导演板自动标注（用户已拍板）

- 漫剧工厂保持五阶段，不新增导演蒸馏页、导演姓名选择墙或第二套流程。策略在大纲后一次冻结，资产、分镜、成片、终审只读同一去名合同的阶段投影。
- 生产 UI、提示词与导出禁止出现导演名、片名和“模仿某某”。优先级为：用户锁定剧本／资产真源 > 用户显式覆盖 > 冻结策略 > fallback。旧草稿没有合同时保持原状，禁止静默重选。
- 分镜导演板必须叠加可编辑矢量层：红色实线为人物／道具路线，青色虚线为摄影机路线，另含入口／出口／主体锚点的空间轴线与接触／碰撞／停顿／结果落点。
- 底图与 overlay 分开保存，坐标归一化为 0..1；只允许拖动端点／中继点和切换图层，不提供自由手绘工具栏。重出底图不删除 overlay；宽高比或构图改变时标记待校准。
- 新分镜在现有请求内产出最小方向结构，使用确定性 SVG 渲染，不新增模型调用。旧稿只有方向明确时才保守编译，含糊时不编造箭头。
- 纯文字参数卡、流程图、架构图和 UI 示意图禁止调用 GPT-image-2 或其他生图模型，一律用 HTML／SVG／React 确定性排版。GPT-image-2 只留给人物、场景、道具、材质、灯光、表演和氛围等真实视觉资产；不是把信息图从 4K 降到 2K。
- 隔离工作树已接通：去名策略五阶段投影；底图与 SVG 轨迹分离保存；红色人物／道具路线、青色摄影机路线、空间轴与落点可拖动确认；确认后的短空间调度进入成片提示词；首段 10 秒零重试门、单镜 Video Edit／版本回退、缺段拒绝最终合成、H3 时长／画质透传与字幕烧录恢复均已实现。
- 本地类型、构建、21 个相关测试文件 199 项和 diff-check 已通过；全仓余 5 项失败均可在 `origin/main` 同源定位到陈旧读片断言。本刀交付为 PR #1372；合并与自动部署状态以 GitHub 记录为准，真实四模型 10 秒付费跑片尚未执行。完整施工与无损回退见 /Users/tangenjie/Downloads/2026Sep04/plans-and-undo.md。

## 2026-09-04 · 人物 3D 可选参考（WaveSpeed 服务端管线）

- 黑奇这类跨集、多机位重复出现的非人角色适合建立 3D 造型／比例参考；跛行属于动态表演语义，不能靠静态 GLB 保证，仍由分镜与视频提示词控制。单张正面透明图先建基础模型，设计锁定后再补侧／背视图升级。
- 3D 只放在「资产」阶段的人物卡更多设置内，不新增第六阶段，不进入默认出片门禁。正常 UI 只写「建立／查看 3D 参考」，不暴露供应商名。
- 生产调用仅走 Fly 服务端 WaveSpeed H3.1 image-to-3D：管理员／监管角色二次确认后提交；密钥缺失在落任务前关闭式拒绝。任务按用户、资产、源版本、规范化参数幂等；未知提交结果转人工对账，禁止自动重发。
- 上游 GLB 必须验 glTF v2 文件头、体积与完整长度后镜像到本人 GCS；草稿同时保留 `gs://` 长期身份与当前 HTTPS 签名地址，刷新页面时由服务端续签。预览 iframe 只接受 HTTPS，并加 CSP 与属性转义。
- 当前仅完成本地实现与虚构依赖测试，未部署、未做真实 3D 付费调用。30 项相关测试、无增量类型检查、Vite 正式构建通过；全仓既有 5 项陈旧读片断言仍红，与本刀文件无差异。

## 2026-09-05 · 创作顾问与无损工作流收口（未上线）

- 漫剧模式右上角使用公开创作顾问，其他页面保留原管理者入口。顾问只读当前集正文、实际资产认领、选中镜头、已生成分镜和项目引擎；未生成 prompt 不冒充结果，长内容明示节选，未看图/听音不声称画质通过。
- 顾问原始问题与包装分离，同一次 requestId 贯穿确认、扣点、jobs CAS、答案持久化、回放和原路退款；通用 worker 不领取顾问 queued 占位。价格、免费额度与管理权限未改。
- 顾问历史按用户和 Bible 已确认版本隔离；未确认稿只作内存免费咨询，不发起付费。坏本机记录先原样隔离，无法隔离或保存恢复编号时停止付费，避免丢回执。
- 同剧导入与确认不清理已有候选/编辑图和主图选择；外形变更标记待复核，生成只读当前 canon 安全资产视图。旧成品归档，轨迹保留坐标但重审；ZIP 同时备份导演板主图、段图与 overlay。
- 旧 v1 导演合同恢复原版不可变快照，不能按同 ID 升成 v2。新 v2 才使用已批准策略的六阶段投影；界面仍为原五阶段。
- 现有 GLB 导入按 GCS generation 校验并条件复制到内容哈希长期路径；预览签名失败保留成功模型身份，只续签不重建。结构合法不等于造型/性能已验收。
- 发现尚未闭环：手动切点只存面板而合成读取另一字段；字幕结果未进入最终画布版本；完整媒体交付包尚非真正视频/SRT/音轨包。后期补接在独立工作树进行，不用示意图或“接口成功”冒充完成。
- 本轮新付费调用为 0，尚未上线；完整测试与分层证据见 Downloads/2026Sep04/漫剧工厂-0905顾问与工作流阶段验收.md。用户保留合并权，线上验收只使用其已经打开的独立 Chrome 测试窗口。

## 2026-09-05 · 后期消费者整合（本地验证，未上线）

- 手动细剪与自动切点共用绝对秒合同并保存源时长；跨集加载不写回上一集状态。漫剧合成保留原声并同步裁切/转场，短配乐不截断成片；集间垫图不插进同集对白。
- 字幕结果按任务和源版本写回，迟到结果只入历史；同一媒体续签保留原片、质检及创建时间。历史列表不再截断至八条。当前及明确归档的整集版本在本机保存与配额降级中保留。
- 换剧备份任一媒体失败则不清旧剧；导演板状态和整集当前/历史同时进入备份。普通成片坞两个导出按钮尚未接到整集历史参数，工具按旧文件边界拦截，已向用户申请明确授权；不得把底层 ZIP 测试等同于两个按钮已可用。
- 同页字幕重复提交被同步锁拦截，跨集轮询计数互不抢先解锁；未知提交结果提示先对账，不诱导重新下单。跨标签页/设备的服务端提交幂等、旧版本跨设备长期恢复、完整视频/SRT/音轨交付包仍未闭环。
- 功能提交 `9df958e`：无增量类型与 Vite 构建退出 0；全仓 4389 通过/4 跳过/2 个既有读片 CLI 失败。新增原声测试实际运行本机 FFmpeg，验证声频、静音、时长与混音，不是只看 HTTP 成功。新付费调用 0，未做线上 Chrome 或真实模型验收。
- 所有来源 Video Edit 统一使用 2.5 编辑引擎，原生成模型不是限制条件。项目生成引擎不因此切换，原片保留；不同模型的真实编辑质量仍待验收。

## 2026-09-05 · 粗剪顺序真实消费（本地部分验证）

- 粗剪两入口改为同批写回各段；源镜号与播放位置分离，物理镜窗不再随重排漂移。新顺序保留到本机、云草稿与合成服务，跨段展开后整集校验再排序；旧稿没有新字段时沿原段序。旧本机自定义序在下一次用户调整剪辑时写入合同，不自动重写所有历史稿。
- 已用本机真实 FFmpeg 验证重排后的红/绿/红画面、660/880/440 Hz 声音与 3 秒时长；不是模型画质验收。全仓 475 文件、4412 通过/4 跳过/2 个既有读片 CLI 失败，最终细节见 Downloads/2026Sep04/漫剧工厂-0905粗剪顺序验收.md。
- 仍阻断完整交付：字幕的实际裁切/转场时间轴与历史整集版本匹配未闭环；普通 ZIP 导出／导入尚未验证保存剪辑合同；当前最终合成服务入口缺专门登录与任务绑定扣费，已请求用户另行授权修复。未合并、未部署、无新付费调用，不能称线上完成。

## 2026-09-05 · 字幕时间码与实际时序核查

- 字幕预览／复制的毫秒进位已与烧字共用格式函数，修复 `59,1000`；新增回归先复现 3 项失败，再连同相关测试达到 7 文件／46 项通过，类型和前端构建通过。此项只修格式，不等于字幕对时完成。
- 已复现同一细剪设置产生 4 秒字幕、8 秒实际合成片段；根因是字幕按计划镜长累计，合成按实际源镜窗映射并计算转场。最终版本尚无实际剪辑时间表／对白快照，旧片可能接收新稿字幕，后续合同与旧稿行为需确认。完整证据见 Downloads/2026Sep04/漫剧工厂-0905字幕时间轴核查.md。

## 2026-09-05 · 已有视频编辑与重拍分流（本地部分验证）

- 编辑误用生成管线有两层证据：旧生成稿/静帧/原片尾帧会混入编辑；仅修出站后，偏好清洗仍会吞掉本次编辑指令。现按编辑契约隔离，只消费所选原片与明确修改要求；同集同段单目标编辑不走上一段续拍门禁，真正续拍不变。
- 普通生成、工作台重拍及节点重跑清除一次性编辑模式和原片绑定，节点编译状态与新 prompt 同批消费；保留全部片段版本和用户独立补充。编辑 POST 结果未知时本次 pipeline 不自动重提，不等于跨标签幂等或失联任务恢复已解决。
- 最后定向 13 文件／181 项通过；全仓复验 4438 通过、4 跳过、2 个既有读片 CLI 上限断言失败；末尾节点透传补丁另由定向回归与类型/构建覆盖。原始失败、修复理由及最终构建回执见 Downloads/2026Sep04/漫剧工厂-0905视频编辑操作边界验收.md。无新付费调用，未做 Chrome 或真实编辑画质验收。
- 编辑质检仍沿用生成静帧/旧段上下文，缺资产的已有原片也可能被生成资产门禁拦住；非最终视频跨设备恢复仍未闭环。上述限制不以单测通过替代。
- 用户本轮要求有推送即开 PR，已为同一分支建立草稿 PR #1384，后续同类推送补进该 PR。用户保留合并权，不自行合并或部署。

## 2026-09-05 · 已有原片的独立编辑入口（本地部分验证）

- 精确同集同段单目标编辑不再生成故事/资产、展开反推、补静帧或按同段重新找另一个 clip；pipeline 只提交所选原片和本次指令。普通生成和重拍保持原路径。
- 新编辑结果回来前保留当前原片、QC、尾帧和全部历史；失败不清旧成品，空结果不冒充成功。新版缺参考时明确 unverified，不能继承旧 passed 或旧手动放行；现有静帧质检不足以证明编辑要求达标，三方对照合同仍待确认。
- 两个通用续跑入口拦住失败编辑，防止清掉 edit 状态后误重跑整集；用户先核对任务，再从原片编辑入口明确确认重试。
- 最终定向 210 项通过；全仓 4454 通过、4 跳过、2 项既有读片 CLI 失败，最后续跑补丁由定向与最终类型/构建覆盖。结果和限制见 Downloads/2026Sep04/漫剧工厂-0905视频编辑独立入口验收.md。未上线，无付费调用；非最终视频本机刷新和云端恢复仍受旧剥离规则限制，不能用运行中保留代替持久恢复。

## 2026-09-05 · 10 秒试片出站合同（本地部分验证）

- 回归复现 duration=10 但独立秒级分镜在末尾重新注入 12—30 秒剧情。现将本次试片选项由工作台透传到实际执行器，只裁切执行副本的主稿与独立分镜，并将请求时长设为 10；普通生成仍按原时长，节点独立分镜原文不改，编辑/延长不能误用试片模式。没有新增供应商字段或持久化合同。
- 实际 pipeline/执行器＋虚构网络边界验证：四引擎请求都为 10 秒；普通生成 30 秒不变；原分镜保留；误用编辑/延长零提交。定向 14 文件/197 项通过，类型与构建原始结果见 Downloads/2026Sep04/漫剧工厂-0905试片契约核查.md。未做真实模型、Chrome 或付费验收。
- 审核尚未闭环：批准键只有集号＋引擎、无账号/项目/视频版本身份；界面不播放待批准 URL；画布单节点运行可绕过审核；保存失败仍提示成功。身份合同已请求确认，未获答复前不改旧持久化结构。试片时长选项不是服务端审批，不能把此项修正当成整个门禁完成。

## 2026-09-05 · 试片服务端审批与真实审核对象（本地部分验证，未上线）

- 用户明确要求继续，同一 #1384 收口上条审批缺口：身份绑定登录用户、确认正文指纹、集号、实际生成档与当前任务；旧本机批准保留但不参与放行。真实播放器使用审核回执 URL，加载失败不允许批准，POST 绑定所看任务；成功持久化后才解锁。
- 工作台、画布与编排器共用预检；Seedance/Wan/H3 服务端在扣费前二次守门，带集段号但漏身份的旧请求不走普通画布旁路。Video Edit 仍豁免新生成试片门，不改项目引擎与既有路由。Mini/Fast 与标准/加长分别按真实档位审批。
- 服务端先持久预留任务/幂等键，并发复用同一任务；审核同任务同决定幂等，反向改判拒绝。未知扣费或建单结果转人工核对，不自动重烧、不自动退款；只有明确未提交的账本登记失败允许创建失败退款。存储坏记录/I/O错误不回落“未开始”，锁回收不抢活锁，不截断任务历史。
- 离线 Chrome 组件测试曾抓到跨项目全局忙标记拖住新项目，已按审核身份隔离并复验；不是在线用户界面或模型画质验收。终审另发现 UI 自动默认档与历史节点实际档不一致：审核、逐集预检、铺段、节点重编译、重拍及合成完整性检查现共用本集解析规则，只接受真实显式选型覆盖。新增回归先红后绿，离线 Hook 验证跨集实际请求 scope。
- 最终定向 20 文件/257 项通过，包含 API 异常 9/9 与浏览器交互 4/4；此前全仓 4504 通过/4 跳过/2 项既有读片 CLI 上限断言失败，冻结链未改。全仓运行早于最后引擎作用域修复，该修复另由最终定向与类型/构建覆盖，不声称最终全仓全绿。
- 最后格式化后重跑仍 257/257；无增量类型、服务端构建、Vite 均退出 0，Vite 23.00 秒且保留既有大包警告。未增加依赖，未做干净 Docker 构建。刷新及云恢复保留 confirmedAt＋规范化正文，正常恢复不会重置审批身份。
- 真实生产卷、四引擎实片质量、真实扣退与用户 Chrome 全链仍未验；片段长期恢复、编辑三方质检、后期合成与字幕/导出待办仍在，不把试片审批增量当成工厂全闭环。完整证据、失败原因与回退：Downloads/2026Sep04/漫剧工厂-0905审批闭环施工.md。新付费调用 0，用户保留合并权。

## 2026-09-05 · 视频引用与任务恢复（本地部分验证，未上线）

- 覆盖前述“普通片段仍被剥离”的代码状态：本机／云草稿现在共同保留普通视频、编辑原片、归档整集的当前 URL、全部历史、任务号／引擎／状态、错误、QC、编辑源／模式、裁切、尾帧和已有整集版本身份。当前为空不能把历史首条补成当前成功片；失败或运行状态不因原片存在变 done。仅保存引用，不是永久视频仓库；供应商临时链接依旧可能过期。
- 新片段恢复必须同时携带原 QC；坏报告不转成缺失报告，保守标记未核验，原文不截断。旧稿确无报告仍沿原兼容政策，逐版本 QC 绑定及原片＋结果＋修改指令三方质检尚未完成。
- 不再按 400 节点／800 边截断备份；超过原有整体容量门则拒绝上传，手动备份给出错误，本机写失败提示不要关闭。配额降级不能清掉视频历史；三键保存任一失败不标新版修订，云上传成功也不能冒充本机保存成功。
- 工作台 `onVideoTaskCreated` 先写编排器工作副本再回调；否则仅写 React 会被后续 publish 覆盖。Freeform 恢复只 GET 原任务，按 taskId／节点输入快照回填到最新状态，跨轮保留同任务快照，终态不被晚回降级。新片进入完整历史，用户改选旧版不被抢回，新片不继承旧质检。缺地址／HTTP失败／业务失败不标完成，不增加自动付费重试。
- 两处异步媒体回灌只替换仍为原对象的节点，避免同 ID 改稿、换剧或新成品被旧缓存覆盖；本机较新时保留本机原稿，不经云白名单再次删字段。
- 最终定向 21 文件／266 项通过，包括原编排函数、连续序列化恢复及实际成片坞消费者；全仓 4533 通过／4 跳过／2 既有冻结读片 CLI 失败，最后加固另由最终定向、类型／构建覆盖。没有实际模型调用、GCS／线上 Chrome 验收或真实媒体画质证据。详细失败与验证回执见 `/Users/tangenjie/Downloads/2026Sep04/漫剧工厂-0905片段恢复验收.md`。
- 同一 PR #1384，保持草稿等待用户合并，不自行部署。回退旧清洗代码前须导出保留完整新格式草稿，不能通过删除任务、媒体、JSON 或本机记录排错。尚余字幕时序／合成计费／完整交付包等断点，不以本次恢复增量宣称工厂闭环。
