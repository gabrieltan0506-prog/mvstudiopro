# PR1451 / PR1452 终审即时记录（2026-09-12）

## 最新授权与复审（覆盖下方历史快照）

用户已明确要求审查后直接修正、提交推送原分支并合并两张现有 PR。基线为 #1451 `179aed1c`、#1452 `d1347f86`。本轮复审修正旧公开 Blob 被一律 410、BGM 已提交但刷新失败时轮询降速、清理器 404 不计请求预算及未知 target 判定、流式 GCS 上传无超时、竖屏透视实体安全边遗漏。旧保留图片读取继续兼容；新公开媒体仅写 GCS。

当前验证及九层边界见 `docs/storage-review-0912/final-review.md`；合并和部署最终回执单独记录，不以下方历史“未授权/未推送”覆盖最新用户指令。

## 历史过程（保留原证据）

用户要求审阅两份交接文件、知识库及最近十张PR，并给出终审和完整修正代码；后续明确授权使用已打开的 Blender GUI 做本机测试，并于 2026-09-12 明确要求删除不必要的 Vercel 存储、降低 Fast Data Transfer、将 Blob 内容迁到 GCS。用户随后澄清指 Blob 文件，不是 /blog 文章。清理与迁移已授权，但删除前仍须核对具体对象、引用和迁移校验；未授权合并、推送、部署或新增付费生成调用。

## 已核真实状态

通过公开 GitHub REST 读取：最近十张为 #1443–1452；#1443–1449 merged，#1450 closed 且未合并；#1451、#1452 open。main=e008e1987831cba5d6da410ebe728dabecf6f9db。

- #1451 HEAD=3938ff3a45d34e0ce5eb477dce764c27f6ba60c1，9文件 +1083/-8。
- #1452 HEAD=fbbc471b6d7d42b30773e73afd5a4a4d296e3664，4文件 +260/-6。
- 两张 GitHub mergeable=true/clean 只说明可自动合并；当前 checks 仅 Vercel Preview Comments success，不能代替编译或实际渲染。
- GitHub CLI API 401，但公开 REST 无鉴权可读；不得因 CLI 失败声称 PR 无法读取。

## 已证实问题

### PR1451：暂不可合并

1. 清理脚本 resolveLiveProductionIds 对 HTTP 200 但 targets 缺失返回空 Set。安全替身复现仍调用删除并退出0，违背保护集查不到即停手。
2. 当前生产/预览保护集只读一次；三轮删除可跨22分钟。安全替身在两轮间把候选b变成当前preview，第二轮仍删b，项目读取次数始终1。
3. 新workflow把 VERCEL_TOKEN 放到 GitHub runner 调上游，与用户本轮生产访问令牌只留Fly的明确规则冲突；下载知识库关于CI Secrets的旧说明不能覆盖本轮指令。
4. 描述“只动一个effect”已过期，实际另改快照、采集器、BGM与清理workflow。1.7k/天不是已证实的防火墙阈值；有进度时轮询仍3秒，不能声称活跃档一律降一个量级。

验证：原HEAD pnpm check退出0；轮询26项+workflow YAML19项通过；清理选择器14项通过。以上未覆盖真实删除链，安全复现没有网络或远端删除。

### PR1452：暂不可合并

1. integration.test.ts 返回报告的局部类型遗漏warnings，194行读它触发TS2339/TS7006。原HEAD pnpm check实际退出2。
2. 新增afterAll递归删除临时目录，包括spec.json/report.json，违反JSON永久保留要求。
3. PR说明仍写扫描成本不随角色数增长、三项集成测试与旧审查轮次，落后于实际代码与后续纠正。

修复副本使用生产PrevisRenderReport类型，移除自动清理JSON；非增量tsc退出0，4文件92项回归通过。原PR分支代码未修改。

## Blender真实验证

- CLI后台 Blender 5.2.1 LTS 在Metal初始化崩溃，退出139；不能据此归因PR。所有我方后台进程调用已返回，未盲杀应用。
- 用户随后打开GUI并明确允许直接操作。主代理在原窗口Python Console执行已审阅runner；先copy=True备份用户场景，结束恢复备份，不退出Blender。
- GUI六组全部通过，耗时8.09054秒：单人竖屏tight、三人竖屏auto、双人strike竖屏auto、马竖屏auto、单人横屏landscape、马横屏landscape。
- 每组48帧逐帧检查真实evaluated mesh顶点，出画顶点为0；不是仅检查头脚报告。首组真实渲染48张540×960 PNG。其余五组是场景构建+网格投影验证，不声称已渲染五组视频。
- 源Python SHA256=cc9c09b331655c16a44fb3f159e582f98e74553f1df1f5239e16123dcf2c1414。
- 完整输入/回执/网格检查/帧SHA：/private/tmp/review1452-gui-evidence/summary.json。

## 保留边界

未实跑正式工厂UI→队列→采用→付费全链、真实积分扣退、生产重启恢复、Linux渲染和多并发峰值；#1444六角色30秒仍是旧实测失败，不因本次构图测试变为通过。没有合并、推送、部署、真实删除。完整终审与完整替换代码已存入 /Users/tangenjie/Documents/Codex/2026-09-06/new-chat/pr1451-pr1452-audit/交付-20260912/终审报告与完整修复代码.md。

## 证据目录

- /private/tmp/pr1451-1452-final-audit/recent-pr-{1443..1452}-{detail,files,reviews}.json
- /private/tmp/review1452-typecheck.log
- /private/tmp/review1452-fixed-typecheck.log
- /private/tmp/review1452-fixed-tests.log
- /private/tmp/review1452-gui-evidence/

## 存储与传输瘦身（新增范围，20:30 记录）

- 旧交接 DTO 方案不能直接采用：旧客户端依赖完整 learning 字段识别原生卡；服务端直接返回 DTO 会把原生卡误分为旧模板，影响批量下架选择。修正为新客户端显式 compact、旧客户端保留完整响应。独立副本 /private/tmp/mvs-transfer-reviewfix-0912，50 项目标测试及非增量 tsc 通过；合成测试 UTF-8 74689→246 字节不等于现网收益。
- 不删除 getProposalDetail 的版本失效：nativeProgress 没有逐帧百分比，updatedAt 与实际内容更新有关；旧资料把它归因为每秒轮询原因不成立。保留 focus 刷新，现有列表没有明确手动刷新替代。
- 远端1451原HEAD仍有Blob写入；本地候选 /private/tmp/mvs-transfer-reviewfix-0912 已将所搜到 api/server 的 Blob 写入生产者全部改为 GCS（原 S3 优先分支保留），Blob SDK仅留旧读取和迁移清点。渲染流式上传，其他原Buffer上传保留；这是本地代码状态，不是线上已迁。
- 旧 Blob URL 已写入任务、云草稿、本机草稿与导出物。不能仅复制 GCS 后删除源对象，否则旧项目会断链；不能把短期签名 URL 永久写入草稿。需要稳定读取地址及引用兼容证据后再清理。
- 本轮权限更新后 Fly CLI 已恢复：status/secrets变量名读取退出0；凭证仍只留Fly。2026-09-12 20:49:47已在原正式机进程内真实清点两store，完整495对象、683930369字节；原“无通道/未列对象”阻塞已被实时证据替代。尚未部署代码。
- 用户截图是 13:41:58 的历史账面，不是本轮实时 Usage；无法由此断言当前对象数或最新用量。Vercel retention 对既有部署的绝对“不回溯清理”说法缺乏依据，不沿用；应以真实候选、保护状态和删除回执为准。

## 20:41 补充的可复核结果

- Blender同输入改前对照已补：改前8.47906秒，改后8.09054秒；单人竖屏真实网格画面高度19.5828%→34.8139%，其余五组投影逐值一致。两轮各只真实渲染单人竖屏48PNG；其他五组不称出片。原始输入/报告/逐帧SHA/场景备份已复制到交付目录blender-before与blender-after。对照MP4为48帧、25030字节，SHA256=3aad0265cbfc7ca21031ce3c97df76160bac409c9da5ebf75949893b8e265b22。
- 新媒体稳定地址使用固定公共namespace+UUID；服务端每次续签并302，不把短期签名永久存草稿。严格拒绝跨目录/跨桶签名；旧Blob读取兼容。下载helper识别blobPath内文件名，保留扩展名。
- 清理候选已修复两项已复现删除问题；每删前重核项目target与部署详情，缺失/未知不删。删除GET→DELETE仍有竞态，必须实际维护窗口。新workflow只离线测试、无生产secrets，真实清理只在Fly。
- 迁移工具默认inventory，--copy后逐对象复制并核SHA/bytes/generation，源变动与目标冲突拒收，续跑重验；不删除旧源。默认64MiB单对象上限、顺序1、媒体预算1GiB；大对象明确pending，不能声称全迁。
- 本地164项存储/DTO/邻接回归通过，另7项直接调用真实API handler的离线测试通过，Vite构建退出0（17.18秒，仍有大chunk警告）。最终整合非增量类型检查退出0，18文件244项回归通过（4.38秒），15项清理离线测试通过（372.08ms），完整补丁对原HEAD应用检查退出0；这些均未调用生产服务。
- 当前GitHub于20:37再读，1451/1452的HEAD不变、仍open未merged。没有线上迁移、删除、推送或部署回执，不声称账面下降。GCS CORS、生命周期和实际URL环境未读到；正式下载/播放器和旧引用迁移仍未验。

## Blob真实读取参数纠正（2026-09-12 21:00左右）

- Fly真实请求确认@vercel/blob 2.3.0的useCache:false追加cache=0后，当前公开Blob返回400；切为useCache:true后，公开/私有各一对象Range+If-Match读取成功，返回ETag等于HEAD。不是Fly登录过期，不需要本机配置生产密钥。
- 初次批量文件头检查失败记录保留在Fly审计目录；没有因检查失败而执行删除。inspection-v2使用默认缓存路径，继续核对ETag并识别二进制媒体头；删除前还会重读HEAD并以ifMatch作单对象条件删除。
- 该问题也影响尚未上线的迁移CLI候选，已同步改为useCache:true，37项目标测试及非增量tsc通过；此前离线测试不证明真实迁移可用。

- 21:04进度快照：495对象已全部完成第二轮内容检查，492项二进制旧媒体通过；1项445字节伪jpg实际SVG单独验证XML后已条件删除；1个0字节download/目录标记待最终核清；9月3日37076字节webp保留。主删除流71项已收到确认、0项结果不明（该数为中途快照，不是最终值）。所有操作在Fly进程内，SDK内部重试关闭，逐项del(url,{ifMatch:etag})，未部署/推送。

## 当前Blob清理结果（21:17:23实时复核，覆盖此前进行中状态）

- 两store495→1对象，683930369→37076字节；移除494对象共683893293字节（492二进制媒体+1 SVG+1空目录标记），oldRemaining=[]，无未知删除结果。八月0；九月3日15:11:31的webp保留。只代表真实对象，不称账单或Deployment Storage已经更新。
- 凭证始终留Fly；每次del绑定ETag且禁SDK重试，原始清单/HEAD/内容判定/删除意图/删除确认/最终分页JSON全部永久保留。证据目录 /Users/tangenjie/Documents/Codex/2026-09-06/new-chat/blob-age-cleanup-0912。
- 新写入GCS实现仍仅本地候选，未push/merge/deploy。用户所要两PR完整单代码块已生成：/Users/tangenjie/Documents/Codex/2026-09-06/new-chat/pr1451-pr1452-audit/交付-20260912/两张PR终审完整修改-单代码块.md，2540行，包含完整修正与GCS替换，无占位；实际远端两HEAD本轮重核不变。
- Fly GCS元数据请求返回403，生命周期/CORS仍无法确认；实际OAUTH_SERVER_URL=https://mvstudiopro.fly.dev，旧“URL环境未知”已被此次读取替代。国内上游GCS可达性仍须验，不以302测试冒充全链。
