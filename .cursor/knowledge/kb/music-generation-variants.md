# 音乐生成候选数量约定

## Mureka 必须保留两首变体

- 用户于 2026-09-12 明确要求“这次不要只生成一首”，2026-09-13 再次确认并要求写入知识库。后续 Mureka 歌曲及纯音乐 BGM 生成，默认一次请求生成两首变体；支持 `n` 的接口必须显式提交 `n=2`。只有用户当次明确要求其他数量时才改变。
- “制作一首歌／一首 BGM”指创作一个作品，不表示把该次生成的候选数量减为一首。一次请求与返回的变体数量必须分别记录。
- 不得擅自设置 `n=1`，也不得以未经核实的“省钱”理由减少候选。用户说明其使用方式中一首与两首同价；此为用户说明，不冒充已查验的供应商账单结论。
- 提交前核对实际请求体中的 `n=2`，不能只在说明里写“两首”。Mureka 与 Suno 对比时，分别列明请求次数与预期变体数。
- 成功后保留并交付全部变体，分别记录供应商任务 ID、候选 ID、实际时长、文件字节数、SHA256 与持久存储位置；不得只下载第一首或擅自挑选一首交付。
- 如果只返回一个候选，先查询同一任务并保留已有结果，报告缺失；不得自动再发付费请求补单。已用 `n=1` 完成的历史任务也不得冒充已补齐。

## 2026-09-13 事故及验证边界

- “国风谍影”BGM 错误提交 `model=auto,n=1`，实际模型 `mureka-9.5`，任务 `160611038265345`，仅返回一首 202.840816 秒音频。
- 本次补记只纠正后续执行规则，没有重新生成，也没有补出第二首。原始输入及回执保留于 `gs://mv-studio-pro-vertex-video-temp/user-requested/bgm-mission-20260913/`。
