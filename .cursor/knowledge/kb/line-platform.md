# line-platform · /platform 内容创作线（动态层，每班收班更新）

- **0901已合并真值（覆盖下方所有候选参数）**：PR #1328 已合并并由冻结 SHA 锁定。Gemini 3.1 Pro 为 `0.7→0.65→0.6`、MEDIUM、无 thinkingBudget/mediaResolution，prompt 与 Schema 不得由 agent 修改；只有分片时长和 fps 可按任务明确指定（默认300秒/12fps，14fps不再测）。三档均未过时从已有永久证据数值择优并标记 `best_effort_unqualified`，不得第四发。GLM-5.3 整形固定0.8/high/131072。真源在 `AGENTS.md`、`shared/manhuaNativeDeepReadJob.ts`、`server/services/manhuaNativeDeepReadRunner.ts`；下方0.65首发、LOW、旧梯度和“待冻结”全部仅为历史。

> 更新：2026-08-17。本线已交接给 UI/功能线 agent，此处存事实供跨线查阅。

## 已上线的关键机制（改动时别打破）
- **逐镜拆片表**：schema `shared/platformStoryboardCells.ts`（7 列，cellIndex 1–12，normalize/fallback/两种格式化器，5 用例）；生产者在 `server/services/platformTopicShortlist.ts` 扩写 prompt；消费端结果卡表格 + 六栏出图文本。
- **扩写双引擎按条计费**：20 点/条 × 勾选数，确认弹窗明示「失败条自动退款」；引擎枚举 稳定档(kimi-k3)/轻快档(qwen3.8-max)，attempts 双通道 fallback，退款幂等（runner.ts expand 分支）。
- **出图断线续航三件套**（#1190 样板，可复用到画布批次）：localStorage 记录（jobId+kind+30min 龄期）→ mount 恢复轮询（posterResumeRanRef 防重）→ finally 清账。PlatformPage.tsx 常量 ~:300、mount effect ~:3190。
- 单页知识卡多页循环 + lastResult 找回（「N 分钟前」toast）。

## 本地已实现（未部署）
- **0831夜最新实测与候选**：84435b3已构建并在指定隔离机单发实测，run `probe_douyin_20260831150352_0a7cc362`返回22镜/13关键帧/16字幕/1音轨/5事件；原始与解析完全一致，但51张原帧核查证实错认、合镜与时间错位，首片质量失败。单发保护已停止，未自动重试。按用户此前条件准备LOW单变量候选，0.65/12fps/prompt/schema/质量门禁不变；实际请求离线差异只有thinkingLevel，LOW尚未付费实测。以下权限和阶段说明保留为较早历史，以用户本场后续明确授权及本条最新实值为准。
- **本轮授权与下一步边界**：用户已明确确认提交并推送到现有PR1328；没有合并、部署或付费调用授权。后续实验失败须先提取原始响应/拒因、分析复盘、改动验证后再重跑。当前代码的三次自动重试尚未调整，下一次实测不能直接沿用该连跑方式。
- **2026-08-31 Codex 时长规则统一（覆盖上一条分区版）**：用户确认继续后，Gemini首发/重试/自检与GLM消费统一单条30秒目标，3秒仅约束同一长镜拆分；真实短镜和相邻等长镜头按原边界保留，禁止虚构变化或改秒位凑规则。保留33秒容差拒收、均长/镜数诊断与原重试决策；修正诊断把均值直接当合镜证据的措辞。0.65/0.65/0.6、MEDIUM无budget、12fps、字数/schema不变；新版本`time-custom-20260831-unified-shot-timing-v1`。目标218项、全仓3836项通过，4项跳过；类型/probe类型/TypeScript及Vite构建通过。旧13镜和62镜JSON改前后重试判定不变，内容质量仍未验收。GitHub PR1328及隔离机2870151a097278仍为e56，尚未提交推送或更新镜像；当前探针要求干净提交对应镜像，真实单片沿现有策略最多3发，需按全局权限明确远程动作后执行。记录在Downloads/2026Aug31/Codex-时长规则统一。
- **2026-08-31 Codex 禁止项集中整理（当前本地状态）**：Gemini 首发/重试共用末尾统一禁止区，搬移混合句但保留原规则；五份实际构造的提示词导出均通过分区核对，20项显式字数上限与schema一致。首发0.65、重试0.65/0.6、MEDIUM无budget、12fps及响应schema均与改前相同；版本改为`time-custom-20260831-separated-prohibitions-v1`，旧缓存指纹不匹配，旧付费JSON保留。全仓3832项通过、4项跳过；无增量类型检查、probe类型检查、TypeScript及Vite构建均退出0。仅本地验证，未推送、未建/部署Fly探针镜像、未新增付费调用；首发3–6秒与重试1–30秒及相邻等长禁令的合理性仍待单独处理，不能据此宣称读片质量修复。完整记录见Downloads/2026Aug31/Codex-禁止项集中整理。
- **时间坐标单变量候选（2026-08-31 02:21后最新口径）**：7d492a4已推同PR并在隔离机核对1028源码/现存5片generation后实跑。单片0.70/0.60/0.55共3发STOP，02:20:21退出0、signal=null，raw/parsed/selected与用量回执完整，估价¥10.356084非账单；第三发39镜/16KM/102字幕通过结构门，但100/165/260原帧均错场，77字幕不在KM±2。第二发字幕419对应原260秒同句，支持分秒拼数问题，另有299等非法钟表值，不允许整份自动换算。下一轮仅澄清生产prompt时间换算，所有生成参数/Schema/门禁保持；候选代码、同源验证与实测尚在推进，不提升两片阶段、不跑后备真人剧。旧五片GLM丢cue/floor/移位阻塞仍未解决。
- **单变量调优与选片诊断（2026-08-31最新口径，覆盖下方历史规格）**：9352588五片9发Gemini后一次GLM已返回，但0.65首发有实际秒位错配，0.60重试大片模板化，消费端另丢46个音轨cue；均未验收。用户授权下一轮仅首发试0.70，重试保持0.60/0.55；319秒/12fps、MEDIUM无budget、Schema/prompt不变。诊断复用生产唯一请求/解析/重试，从完整1594秒5片清单选择原索引，先1片，稳定才2、3片，不逐片GLM、不写半集卡。当前新候选待最终回归、同PR推送及隔离机实跑，不合并、不部署正式站点、不冻结。下面300秒硬顶、四并发、旧温度梯度和未发车状态均为相应日期历史，不得作为新实验参数。
- **medium统一（2026-08-31最新指令）**：Gemini为MEDIUM，GLM整集与系列均改medium，两份旧探针显式复用生产常量；其他参数不动，旧max聚合缓存因参数指纹不同不复用。当前五片实测只做5次Gemini+1次整集GLM（正常无重试），不额外系列聚合；待实测后再冻结。
- **五片验收状态（2026-08-31）**：原速319秒/12fps、Gemini/GLM相关链路均medium，3682项与类型检查通过的efa47b7已推送；先实测后冻结。发车前补GLM原始/解析与探针完整结果永久留存，用户明确允许现有桶指定前缀保存、不公开；该增量仍待统一验证及同源镜像。真实模型质量未验，不得以备料或离线绿灯代替。
- **原生精读自定义时长与采样（2026-08-30，PR1328追加，未推送）**：学习面板新增 `nativeSegmentSeconds`、`nativeVideoFps`，默认300秒/10fps，可独立设置；用户当前测试选择319秒/12fps，不自行推导自动档位。设置贯穿 Job、worker、计划、执行、重试、缓存指纹、服务端恢复及本地续跑；只影响新任务，不改运行中 Job。整片120分钟、每集32片、调用数保险保留，单片300秒硬顶与长片自动5fps已取消。视频采样接口上限为24fps；30秒镜头证据段上限不变。生产切片改精确seek+CRF18音画重编码，原尺寸原帧率，真实尾片读至EOF；每片检查时长与局部起点，GCS复用探针也调用同一验收函数并永久保存原始/解析元数据。版本 `time-custom-v22-exact-cut-configurable-fps` 避免复用旧错位缓存，旧JSON不删除。真实合成样片210帧在三片中连续无前滚、重复或漏尾，有声无声均通过；真实漫剧尚未用新代码切片或调用模型。1.25/1.5倍速仅讨论，现有解析链尚无倍速时间映射，未实现。
- **学习来源解析扩展（2026-08-28，待部署验收）**：带 `modal_id` 的抖音搜索页继续先归一到稳定 `/video/<awemeId>`；没有官方 `mix_info`、但详情明确免费且存在可信媒体流的长视频按单独一集进入原生精读，身份绑定 awemeId，不以标题串库。新增 0996zp / gzcrkt8888 同构播放页来源：SSR 分集目录锚定真实集号，匿名可用 480p HLS 进入既有 300 秒分片、claim、段缓存与一集一张待审卡链；登录专用 720p/1080p 不取。片头片尾秒位仅保存在 provenance 供审批查看，绝不自动剪除。额外镜像只能由 Fly 服务端精确域名白名单放行，重定向、DNS 与媒体域逐层关闭式校验；未使用本机凭证或付费模型调用。
- **原生精读五分钟失联根因修复**：线上两次 `fetch failed` 分别发生在 326 秒和 342 秒；代码虽然有 30 分钟业务总时限，但 Node/Undici 仍使用默认 300 秒响应头时限。现为该模型请求单独配置 30 分钟 headers/body timeout，仍由原 AbortSignal 负责总时限；同时把底层 `cause.code/name/message` 写入 owner 回执。Growth Backup 已有互动租约让行机制，但 Job runner 原来只给 `platform` 任务建租约，现把真实登录用户的 `video/manhua_template_learn` 纳入同一租约，避免学习期间同机冷备拉取与打包大文件。
- **原生精读长请求与学习面板稳定性**：非流式 Qwen 请求的 socket idle 由 120 秒改为 10 分钟，总时限保留 30 分钟；任务轮询不再依赖整颗 tRPC query 对象，无变化的服务端 Job 快照复用旧数组引用，避免 3 秒轮询退化成无间隔 GET 并重绘原生下拉。静态与全量测试已过，Fly 旁路真跑待执行。
- **原生精读占位与刷新恢复**：历史 claim 继续隔离但不再挤占“学习 N 集”的名额，计划确认仅在执行清单与 claim 重叠时拒绝，逐集调用前仍原子抢 claim。终态失败在当前会话可见，刷新后清掉失败自动焦点并回正常入口；运行中与成功待续仍恢复。静态与全量测试已过，线上面板真跑待执行。
- **原生精读面板真值热修**：`/platform` 的 AI 漫剧学习区按 owner 能力显示原生视频精读说明、直接开始按钮与自由批次范围；点击后直接建立持久任务，不再先走前台预演。抖音原生候选在权限读取失败或 owner 不匹配时关闭式停止，不再静默建立旧抽帧任务。历史任务按 `pipelineMode` 明示「原生精读／旧抽帧」。
- **原生精读 300 秒四片与页面收口**：生产分片固定最长 300 秒，约 18 分钟素材拆 4 片；取消旧 64MB 整集媒体预算与预转码，每片原样上传 GCS、独立调用。当前学习页面只恢复原生精读任务，点击后开始态立即写 `native_deep_read`，不再先闪旧抽帧页面；模型返回只显示“正在校验”，门禁拒收会先写明拒因再重试。300 秒以内固定 10fps；视觉首轮温度 0.7，后续两次为 0.65 / 0.6，不得归零。模型徽章固定显示 Gemini 3.1 Pro。
- **原生精读分片重试与透明回执（2026-08-27 施工中）**：同一 Vertex 分片所有失败统一最多三次，固定温度 `0.7 → 0.65 → 0.6`、两次间隔各 60 秒；用户中止不重试，也不得静默换供应商。每次模型请求均记录 started 与 completed/failed，包含尝试序号、温度、HTTP/底层网络 cause、request id、finish reason、token、费用与耗时。单集媒体备料和模型调用各最多四并发，跨集仍串行；任一 worker 失败后不再领取新段，但等待已在途任务收尾并统一清理。GLM 5.3 整集结构化输出上限固定 131072，真实四分片聚合曾在 12 分钟客户端边界被中止，现只把单次等待放宽到 30 分钟，不自动重提。
- **首张新模板范本**：系列 `36a7c84f485b` 的第 1 集原生精读成果已直接写入 `approved/tpl_native_36a7c84f485b_ep001.json`；包含 96 镜、115 条字幕，进度为 1/4。它虽来自一次 360 秒分片，但属于原生视频精读的新模板成果，继续保留；后续发车仍统一按 300 秒分片，并从同一来源补全。
- **原生精读全量证据与永久原稿（2026-08-28，本地验证）**：成功分片先把未经消费者缩减的原始模型 JSON 永久写入 GCS `segment-evidence/`，集卡 provenance 保存对象名；续跑缓存命中也会补齐永久证据。已删除 `beatGrid=128`、字幕 512、音轨/事件 128、场景/标签/来源等固定数量上限，以及旧帧链和消费端的均匀抽样/合并压缩；同集覆盖与系列整理现在保留全部结构。容量不足只能显式失败或走已有 fallback，不能静默截断。当前仅完成静态、目标与全量回归，未做线上付费学习与 GCS 真写入验收。
- **真人剧招商广告证据分轨（2026-08-28，本地验证）**：Gemini 镜头 schema 新增必填 `evidenceRole=story|non_story_ad`；只有模型明确判断为“与剧情无关”的招商广告、贴片、品牌口播或品牌落版才标 `non_story_ad`，其余镜头统一为 `story`，不增加其他推断条件。两类镜头都完整保存在永久原始 JSON 和全片时间轴；`non_story_ad` 不计剧情镜头密度，也不进入剧情字幕、`beatGrid`、节奏/情绪/分类/可复用手法或生成提示。音轨原始证据仍完整保留，只禁止广告声音进入可复用声音总结。旧批准卡读取时缺字段默认 `story`，不破坏历史库。尚未做线上付费真跑。
- **独立角色站位与表演证据（2026-08-28，本地验证）**：0827 已批准探针契约中的 `unitTypeZh` enum（`剪辑镜头|拆分镜证据段`）、`compositionZh`、`blockingZh`、`bodyActionZh`、`limbPropActionZh`、`microExpressionZh`、`gazeBreathZh`、`relationshipReactionZh` 曾漏迁生产，导致站位和表演仍混在 `actionZh`。现已接入 Gemini response schema、缺字段门禁、段解析、卡片解析、系列聚合、模板优化防丢、审批展示与编剧注入；编剧入口也取消 8000 字静默截断。声音 `cues.kind` 使用共享的 11 项 enum（声源/发声/音效/配乐/环境/动态/混音/静默变化），生产者、解析门与探针同源。历史卡仍允许缺字段；新产出缺任一栏会明确拒收。视觉计划版本升为 v8，旧段缓存与确认码自动失效。尚未做线上付费真跑。
- **批准模板 owner 查看/优化**：入口位于 `/platform` → AI 漫剧 →「模板库（已批准 · 编剧室可选）」。完整库、单卡详情、原始 GCS 列表、优化调用与修订批准都以 `OWNER_OPEN_ID` fail-closed；角色和监管会话不能替代 owner。
- 优化结果只落 `manhua-template-learn/proposals/`；owner 批准后旧版先归档到 `archive/`，再以原内部 id 与 publicCode 替换 `approved/`。`/canvas` 未改，继续只读匿名 `listApprovedPublic`。
- 模型档：Terra High、Kimi K3 Max、Claude Opus 5 High、DeepSeek V4 Pro 0813 High。DeepSeek 不传 temperature/top_p，固定 65536 + JSON + `require_parameters=true`。未做付费实跑或线上 GCS 写入。

## 待执行（已批口径）
1. CREDIT_COSTS 双源合并（shared/plans.ts + server/plans.ts → 单真源）。
2. 旧同步扩写 mutation 下线（routers.ts expandPlatformTopicPicks 同步版）。
3. 5 处复制按钮**拿掉**（不许拿报错当交付）：GodViewPage:249 / VideoParserWidget:248 / HomeMyWorks:167 / ManhuaEditMultitrackPanel:399 / ManhuaScenePropDemoStrip:236。
4. 出图 90s 超时滑贵价通道：服务端修超时（用户侧通道选择已否决）。

## 待批
- 长文→图文知识卡（提炼 25 点/次 + 按页现价），全案 `~/Downloads/2026Aug12/功能提案-长文转知识卡.md`。

## 已否决（别再提）
- 用户侧出图通道下拉；B 线媒体水印（当期）；Neon 迁移。


## 2026-08-31 夜：前置验收契约，恢复MEDIUM基准待单发

用户最新要求先将门禁要求写入prompt/schema，再以0.65/MEDIUM/12fps/65536同片单发；d60224f的LOW候选未构建未实测，留在历史。当前把按段长的镜数/均长要求、story17字段/广告3字段、关键帧剧情精华与同秒画面核实前置，动态schema进入实际请求、探针审计与缓存指纹。镜头与keyMoments分开计数，不能合计当镜数。全仓3844通过/4跳过，类型与探针预检通过；此刻线上内容未验证，不宣称修好。证据：Downloads/2026Aug31/Codex-前置验收契约。
