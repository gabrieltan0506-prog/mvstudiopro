---
description: 部署與協作邊界 — 線上生效、Fly 表述、任務閉環（與 docs/DEPLOYMENT_BOUNDARIES.md 一致）
alwaysApply: true
---

# 部署與協作邊界（Cursor / Agent 必守）

**完整條文**：[`docs/DEPLOYMENT_BOUNDARIES.md`](../../docs/DEPLOYMENT_BOUNDARIES.md)。以下為執行時速查；使用者約 **每 10 小時**可能對照提醒，**不得當成可忽略**。

## 線上司實

- 使用者要 **線上生效 / 推送部署 / 要能上網址驗證** → 必須做到 **遠端有對應提交（push 分支或依流程合併）**，**不可**只在本地 `checkout`/`reset` 就結束。
- `git push` 失敗 → **立即回報錯誤與已做嘗試**，不要假裝完成。

## 部署通道（兩條，缺一不可）

- **API 走 Fly**（`main` → Actions `Fly Deploy`）；**www 前台走 Vercel Git 自動部署**（`vercel.json` 已開啟）。**兩條各自發版**：Fly 綠不代表前台上線了。
- **改動含前端**（`client/**`、`shared/**` 被前端引用的部分、`vercel.json`、`client/public/**`）→ 必須確認 **Vercel production 為 success**，並實際 `curl` 正式域名核對改動已生效；只看 Fly Deploy 綠就宣稱上線 = **未完成**。
- **Vercel production failure 不可忽略**：可忽略的只有 **PR 上的 Preview 紅燈**（含 rate limit）。production 紅 = 前台整個停更，後續合併的前端改動全部不上線。
 - 2026-08-05 23:44 起因一行 `buildCommand` 寫壞，production 連續失敗**六小時**無人發現；期間四張 PR 照常合併、Fly 全綠，用戶自己撞見 `/blog` 404 與導航沒改才暴露。事後加了 `Frontend Deploy Check` workflow 自動報紅，但**人也要看**。
- 對方明文要求時可操作 Fly / `fly.toml` / Deploy workflow。

## 任務閉環

- 指令若含 **push / PR / 合併** → **少一步即未完成**。
- 不用空泛「邊界」「安全」回避已聲明的交付。

## PR

- 與 [`git-pr-workflow-always.mdc`](./git-pr-workflow-always.mdc) 一致：常規走分支 + PR，非緊急不直推 `main`；**同一自然日開 PR ≤ 20 次**。

## 視頻探針

- 與 [`seedance-probe-always.mdc`](./seedance-probe-always.mdc) 一致：測試探針一律 **Seedance Mini · 480p**。
