# 协作口径（新 Agent 必读）

合并自旧的 `user-instruction-priority` / `ask-where-to-start` / `ask-before-design-choices` / `knowledge-base` 四条。规则每轮都进上下文，所以只留硬约束，不写教条。

## 一、开场（硬）

接手或新开会话，**先读再问，不要直接动手**：

1. 本文件 + 另两条 always 规则（`ship-always`、`product-guardrails-always`）
2. [`.cursor/knowledge/README.md`](../knowledge/README.md) → [`manhua-factory-brief.md`](../knowledge/manhua-factory-brief.md)（北极星 / 成片验收 / UI / 代码路径）→ [`PROGRESS.md`](../knowledge/PROGRESS.md) **最新日期段**
3. 七月考古才看 [`downloads-jul2026-index.md`](../knowledge/downloads-jul2026-index.md) 与 [`PROGRESS-2026H1-archive.md`](../knowledge/PROGRESS-2026H1-archive.md)

读完**用一两句复述当前进度**，再问「这轮从哪开始、怎么推进」，等回话才动手。

在用户回话之前只允许**只读排查**（读代码 / git log / 看 PR 与 CI 状态）。禁止任何写操作、生成、生图、`git push`、`gh pr create`、`gh pr merge`。

**例外（可直接做）**：用户本轮已明文给出任务或起点（「改静帧门禁」「合并 #NNN」「继续昨天的 L2」）；或明文说「你定 / 按你判断 / 别问了」。但范围要扩大、要烧积分、要删资产、要改对外口径时，仍按第三节先确认。

动漫剧成片、画布、可拍表相关的活，动手前至少读完 `manhua-factory-brief.md` §2–5。

**跨影响（硬）**：动手改功能前必须遵守 `ship-always` **§2.4**——先搜全调用方、预判邻接会不会双扣/盖旧图/旧草稿炸掉，再改代码。口头「我下次会注意」无效；回报缺邻接核查清单不得称做完。用户 2026-08-07：认错三秒后又单点瞎改，必须写进规则才算数。

## 二、用户明文优先（硬）

- **用户最新一条明文指令**优先于默认门禁、旧回合习惯、以及 Agent 自己的「稳妥推断」。
- 例：说「只 push 不开 PR」→ 禁止 `gh pr create`；说「开 PR、不准合并」→ 开完就停；说「合 #NNN」→ 才可 `gh pr merge`。
- 门禁（串行 PR、15 分钟间隔、Deploy 绿再合）在**没有**明文覆盖时仍生效；有覆盖时以用户为准，并简短说明「按你的指令覆盖门禁」。
- 前后指令打架时：**先用一两句复述「当前要执行的那一条」**再行动，禁止闷头按更早那条做。
- 被门禁挡住时不要只甩「不允许」，要给出符合当前指令的下一步（例如：已 push 的分支名 + 链接，并写明未开 PR）。

## 三、设计选择题先问（硬）

出现**多种合理实现路径**且选择会影响产品语义、文案口径、审核边界、数据结构或用户可见行为时，禁止自己拍板后直接改代码。典型场景：产品口径分歧、软边界 vs 硬禁令、库/名单扩容范围、用户可见格式变更、默认示例与偏置取舍、两套架构都能跑。

做法：用简短中文说清分歧（1–3 句）→ 给 2–4 个选项，每项写清做什么、不做什么、对产出的影响 → 可附推荐项与理由 → **等用户选择**再动手。信息不足先提问补齐，不要猜完就写死。

**例外**：用户已明文选定或说「按你推荐做」；纯机械修复（typo、tsc、明显 bug、与既有规则一致的小补丁）；用户要求「先实现一版再调」且范围已框死。

## 四、冲突裁决与留痕

- **现行真值** = `manhua-factory-brief` + `PROGRESS` 最新日 + 用户本场明文。
- `~/Downloads/*` 与 `docs/2026Jul*` 只作考古，与简报冲突时**信简报**。
- 改完一块模块：追加 `PROGRESS.md` 当日行；口径变更同步改 brief §2–3。

## 五、态度

回复用中文、短句。产品验收上的火（假锁角色、废话提示词、布局不对）当硬需求修，不辩解。少讲流程，多报可核验结果（PR 号、分支、Deploy 绿红）。
