# 验证 · Git · 部署（硬）

合并自旧的 `local-verify-before-push` / `git-pr-workflow` / `deployment-boundaries` 三条。**用户本轮明文可覆盖**（见 `collab-always` 第二节），覆盖时要点明依据。

## 一、本地验证门禁

在 **`git push` 功能分支**、**`gh pr create`**、或回报「改好了 / Deploy 该绿」之前，必须先在本机跑通：

1. **`pnpm check`**（`tsc --noEmit`）——与 Fly 构建同门槛。只跑过某个 vitest 就宣称可合是违规。
2. **相关 vitest**：改到的模块对应 `*.test.ts`；触及共享工厂或画布布局时补跑 `canvasDramaStudio` 与 `manhuaSeedanceLayout`。

回报里要写清**跑了哪些命令、通过还是失败**。没跑就不许说「可合」。Deploy 因 `tsc` 变红视为本门禁失守，热修分支同样先 `pnpm check` 再推。

**例外**：纯文档 / rules / `PROGRESS` 且不含 TS 改动，可跳 vitest；用户明文说「先推别测」可跳，并在回复点明。

## 二、防空壳验收（硬）

`tsc` 绿只证明形状对，不证明里面有值——空壳最擅长过这一关。2026-08-06 的 PR③ 就是实例：`propImageUrlById` 类型、参数链、七个调用点全接好，`pnpm check` 与 vitest 全绿，但**没有一处构造这个 map**，它永远是空对象，整张 PR 的价值归零。画布多年「按钮亮着、一点就失效」，根因同一条：只改表层、不追多层接线。

### 2.1 三条门禁（缺一即未完成）

1. **倒着追生产者**。从最终消费点（API 入参、扣费体、写入节点的字段、模型 prompt）往上找「谁真的往里写值」。消费者与参数链谁都写得出，**生产者才是活证据**。找不到生产者 = 未完成，不管测试多绿。
2. **验收写端到端真实数据，不写「功能已实现」**。要写成空壳报不出的数字或具体值：「导入六集包得人物 18 张、场景 6 张、无重复」「裁切 1672×941 得 1291×670」。写「支持 ZIP 导入」「裁切函数可用」等于邀请空壳。单测同理：断言具体值，不只断言不抛错。
3. **回报要贴实际输出**。跑出来的打印、断言到的数值、HTTP 状态码、编译前后 diff。「已完成」不算完成，看不到输出的完成一律退回。

### 2.2 禁止用「看起来美好」代替验收（硬）

- **禁止**拿架构图、流程图、调用链示意图、精美说明当交付物。图可以辅助自己想清楚，**不能**代替生产者追查与实际输出。用户明文（2026-08-07）：画图事后验收惨不忍睹，要求写进规则而不是交图。
- **禁止**只改 UI / 只加按钮 / 只加类型与可选参数，就报「接上了」。按钮从点击到生效，必须沿多层对齐：页面 handler → lib 编排 → shared 编译/绑定 → server mutation 或 job → 回写节点/草稿。任一环断了 = 整条失败。
- **禁止**用「参数已透传」冒充接通。透传空值、默认 `{}`、可选参数省略后静默变空，一律算未接通。

### 2.3 画布 / 按钮类改动额外硬约束

触及 `OmniCanvas`、`ManhuaScriptWorkbench`、`canvasRunBlock`、`canvasDramaStudio`、资产锁与成片提示词时：

1. **点一下必须沿链追到底。** 从该按钮的 onClick / mutate 入口，逐文件追到真正发起出图/成片/写库的那一层；回报里写清路径上的文件与关键赋值（谁写了 `prompt` / 谁构造了 map / 谁扣了费）。只说「已接线」无效。
2. **重跑必须重编译。** 节点重跑不得复用节点里存的旧 `block.prompt` 假装吃到了用户最新改动（见 `OmniCanvas` 内「画布上点节点重跑只会复用旧 prompt」注释）。验收必须贴出**改字段前后两份编译产物的 diff**。
3. **对照与回退同批。** 若行为变更会丢掉旧生成结果，必须同批交付：旧图进暂存区、可对照、可恢复到节点上；禁止静默覆盖用户已有成品图。不保留「按旧提示词原样重跑」旁路（用户 2026-08-07 明文「不要留」）。
4. **空值必须惰性。** 新可选依赖为空时，不得改变既有成片/出图文案与预算；有值时才进位。用单测钉住「空 / 有」两支。

**最危险的一类**：结果肉眼可见但正确性不可见（按钮转、图出来了，其实还在用旧 prompt）。空壳化之后用户只会以为模型没听话。这类没有前后 diff = 未完成。

### 2.4 禁止单点无知编程（硬）— 口头认错无效

用户 2026-08-07 明文：Agent 认错通常只有三秒有效，之后又只改被点名的那一处，不推理邻接失效；Claude Code 贵在会先调阅相关代码并预判踩坑。下列约束**写进规则才算数**，会话里的「我以后会…」一律不可信、不可当验收。

**动手前（写代码之前）必须做完，否则禁止开始改：**

1. **搜全调用方。** 对将要改的字段 / 函数 / action / prompt 键，用搜索列出读它或写它的文件（至少前台 handler、lib 编排、shared、server/job、草稿同步）。报不出名单 = 还没资格动手。
2. **预判失效面。** 明确回答并写进当轮计划（可极短，但必须有）：改完之后，相邻路径会不会双扣费、静默盖旧图、旧草稿反序列化炸掉、空值改变既有成片文案/预算、另一条按钮仍走旧逻辑。答「应该不会」而无可查文件依据 = 不合格。
3. **范围锁死不等于思维锁死。** 代码改动可以只落在用户点名的功能；**推理范围必须覆盖会吃同一数据的邻接功能**。禁止「你要改 A 我就只打开 A 文件」。

**回报「做完 / 可合」时必须具备（缺一条就退回）：**

- 列过的调用方 / 邻接路径（文件路径即可，不要长文）
- 为何不会双扣、不会静默覆盖用户成品、空值是否惰性——各用一句指向具体代码
- 防空壳三条（生产者、真实数据断言、实际输出）仍然满足

**禁止：** 用道歉、保证、复盘语气代替上述清单；禁止把「只改一处」当成优点，若未证明邻接未伤。

## 三、Git 与 PR

- 写完一小段就在**功能分支** commit 并 `git push`（远端备份），**不要**每推一次就开 PR。
- 攒到**一整块模块**告一段落再 `gh pr create` 对准 `main`。非紧急**不直推 `main`**；用户明文要求紧急热修才可以，并说明原因。

### 3.1 开 PR 前强制查旧 PR（硬 · 事故 #1114/#1115）

**任何** `gh pr create` 之前，必须先跑并贴出结果（至少摘要），禁止凭记忆声称「没有旧 PR」：

```bash
gh pr list --state open --author @me
gh pr list --state open --search "<当前主题关键词>"
gh pr list --head "$(git branch --show-current)"
```

判定（命中任一条 → **禁止** `gh pr create`，只能往已有 PR 的分支继续 push）：

1. **同分支已有 OPEN/DRAFT PR**（含 draft）
2. **同主题 / 同一波交付还 OPEN**（例：首页照片工具、Seedance 2.5 上线宣传、Racemow 样片——必须叠进同一张，不得另开）
3. **上一张自己开的功能 PR 仍 OPEN**（未 MERGED）

**禁止：**

- 嘴上说「下次先查旧 PR」却不跑上面三条命令就 `gh pr create`
- 同一波改动拆成 #1114 + #1115 这种「挤掉 / 拆开」原 PR
- 用「只是小修」「倒计时而已」「已经合了再开一张」当借口绕过

用户当轮明文要求「必须另开一张」才可例外，并在 PR 正文写明例外原因与关联旧 PR 编号。

- **节流**：两次 `gh pr create` 间隔 **≥15 分钟**（墙上时钟）；**同一自然日 ≤20 次**。到点不是鼓励开空 PR，模块优先。
- **串行门禁**：上一张 **MERGED** 才可开下一张；且上一张在 `main` 的 **Fly Deploy 必须 success** 才适合合下一张（`cancelled` / `failure` / `in_progress` 都不算，刚 MERGED 十几秒就合下一刀会把上一刀 Deploy 顶掉）。
- **禁止默认代合（硬）**：默认不许 `gh pr merge`。开完 PR 只报告链接与 CI / Deploy 状态，**等用户当轮明文**说「合 / 帮我合上 / 你来合 #NNN」才执行。用户说「停 / 别合 / 我自己合」立即停。
- PR 描述用简短完整句：改了什么、为什么，必要时附 issue 或 run 链接。

## 四、部署真值

- **API 走 Fly**（`main` → Actions `Fly Deploy`）；**www 前台走 Vercel Git 自动部署**。用户要「线上生效」→ 必须**远端有对应提交**，只在本地 checkout/reset 不算完成；`git push` 失败要立刻回报错误与已做尝试，不许假装完成。
- **合并 ≠ 部署成功**：`MERGED` 不够，要查对应 Actions 是否 success。红或 cancelled 视同未完成，等下一次含该提交的 Deploy 绿，或自己修热修分支再推（合并仍需用户明文）。
- **前台是另一条通道（2026-08-05 事故教训）**：只看 Fly 绿不够。曾因 `vercel.json` 的 `buildCommand` 挂了博客生成，导致 www 停更六小时而全程无一处报红。现有 `frontend-deploy-check` workflow 看门，但涉及 `vercel.json`、构建脚本、静态产物时仍要单独确认正式域名可访问。
- PR 上的旧 Vercel 红灯不作为 merge 门禁；正式域名以 production 为准。
- 指令含 **push / PR / 合并** 时，少做一步即未完成。不要用空泛的「边界」「安全」回避已声明的交付。
