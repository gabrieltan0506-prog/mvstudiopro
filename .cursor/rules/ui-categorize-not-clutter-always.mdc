---
description: UI/能力入口必须按用途分类排布，禁止扁平乱堆
alwaysApply: true
---

# 分类排布（禁止混乱）

新增模板、Skill、Tab、动效库时：

1. **先定分类再挂 UI**：一类一个篮子（如 文案 / 动效PPT / 素材；人像 / 生活 / 场景 / 风格）。
2. **组内固定顺序**：用 `*_ORDER` 或显式 id 列表；禁止按「后来的追加在末尾」导致同类打散。
3. **同屏分区**：多入口时用小标题或分隔条标「文案 / 动效PPT / 素材」，不要一条 chip 墙混排。
4. **Skill 池**：笔记节奏 ≠ 一键模板 ≠ 动效PPT ≠ 视频广告；分别进 `graphic` / `templates` / `deck` / `video`。
5. **反例**：把动效PPT、Image-2、封面 Skill 全塞进「图文笔记」或「一键模板」；10 个模板无分组横排。
