---
description: 全站悬浮按钮分区，避免互相遮挡点击
alwaysApply: true
---

# 全站悬浮区（必守）

新增任何 `fixed` / 悬浮按钮前，先避开已占用区：

| 区域 | 用途 |
|------|------|
| 顶栏全宽 | `Navbar`（z-50） |
| **右上**（`top ≈ 4.5rem`） | 管理者 **Pro Agent** |
| **左下** | PWA「新增到手机桌面」 |

**禁止**再把新 FAB 放到 **右下角（bottom-right）**——曾挡住 Pro Agent / 主操作。

若必须新增悬浮入口：优先放进 Navbar / 页内工具条；否则选「左上 / 右下以外」并在 PR 说明避让理由。
