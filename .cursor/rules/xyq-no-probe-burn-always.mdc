# 小云雀积分保护（硬 · 用户 2026-08-03）

> **覆盖关系**：用户本轮明文优先。

## 禁止

- **禁止**用小云雀 / `XYQ_ACCESS_KEY` / `Seedance_2.5` 做「连通探针 / 探活 / 5s 试打 / connectivity probe」。
- **禁止**在未获用户当轮明文同意时提交会扣小云雀积分的 `submit_run` / `generate-video`。
- 连通性只允许：查 secrets 是否存在、读文档/CLI 形态；**不要**真实出片。

## 成败判定

- 小云雀侧已出片（网页创作历史有记录）= **成功**，即使我方 API 返回 `fetch failed` / 没拿到下载链。
- 我方超时或镜像 GCS 失败时：须回报 `thread_id` / 网页会话链接，**禁止**只报失败、让用户以为没生成而重打（双倍烧积分）。

## 与 Seedance 探针档关系

- 仓库内「工厂探针」仍走 OpenRouter **2.0-fast · 480p**（见 `seedance-probe-always`），**不得**改打小云雀 2.5。
