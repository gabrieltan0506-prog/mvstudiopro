# 产品护栏：对外口径 · UI · 烧钱闸门（硬）

合并自旧的 `frontend-no-tech-leak` / `ui-categorize-not-clutter` / `floating-ui-zones` / `seedance-probe` / `xyq-no-probe-burn` 五条。

## 一、前台零技术泄漏

适用于**终端用户看得到的一切**：UI 文案、Toast、空态、帮助提示、导出物说明、创作顾问回答，以及 Prompt 里要求模型「对用户说的话」。

**禁止出现**：模型名与部署名（GPT-×、Claude、Gemini、Terra、Sol、NB2、Nano Banana、Veo、Omni 等）；供应商与云厂商（OpenAI、Google、Anthropic、EvoLink、Vertex、Fly、Vercel）；内部引擎代号、路由名、Skill 文件名堆砌；「仿写某某导演 / 致敬某某」这类点名口径（手法可用中性标签，不写人名）；Debug / supervisor 专用控件文案漏进普通用户路径。

**允许**：`debugMode` / supervisor / admin 面板内的技术标注；服务端日志、代码注释、内部 docs 与 rules。API 报错映射给用户时只给业务友好句（如「算力紧张，请稍后重试」），原始 upstream 进日志。

写法示例：「本页使用 GPT-5.6-Sol 生成」→「正在生成文案…」；「Vertex googleSearch 失败」→「联网摘要暂不可用，已改用库内样本」；「仿诺兰式运镜」→「长镜头压迫感 / 时间折叠剪辑感」。

新增 `toast.*` / 按钮文案前自检：截图里被普通创作者看到，是否看得懂且无供应商痕迹？必须区分引擎时只放 supervisor 开关，默认文案保持中性。

## 二、UI 分类与悬浮区

新增模板、Skill、Tab、动效库时：**先定分类再挂 UI**（一类一个篮子）；组内用 `*_ORDER` 或显式 id 列表固定顺序，禁止「后来的追加在末尾」把同类打散；多入口时用小标题或分隔条分区，不要一条 chip 墙混排。Skill 池分开进 `graphic` / `templates` / `deck` / `video`。

悬浮区已占用：顶栏全宽是 `Navbar`（z-50）；**右上**（`top ≈ 4.5rem`）是管理者 Pro Agent；**左下**是 PWA「新增到手机桌面」。**禁止**把新 FAB 放到**右下角**（曾挡住 Pro Agent 与主操作）。必须新增悬浮入口时优先放进 Navbar 或页内工具条，否则选右下以外的位置并在 PR 说明避让理由。

## 三、视频探针：只用便宜档

- 凡「视频生成测试探针 / factory probe / seedance-probe」一律 **Seedance 2.0 Mini · 480p**（约 5s）。工厂探针 env 默认 `CANVAS_PROBE_SEEDANCE_VERSION=2.0-mini`、`CANVAS_PROBE_SEEDANCE_QUALITY=480p`，代码默认对齐 `shared/seedanceEvolinkModels.ts` 的 `SEEDANCE_PROBE_DEFAULT_*`。
- **禁止**把探针默认改成标准 `2.0`、`720p`/`1080p`/`2K`/`4K`、或 `2.5`。
- 仅当用户**明文**要求全价联调或验收成片画质时才可临时改档，并在回复写明原因与恢复计划。

## 四、小云雀（Seedance 2.5）积分保护

- **禁止**用小云雀 / `XYQ_ACCESS_KEY` / `Seedance_2.5` 做连通探针、探活、5s 试打（一发约烧 130 积分）。
- **禁止**在未获用户当轮明文同意时提交会扣小云雀积分的 `submit_run` / `generate-video`。
- 连通性只允许查 secrets 是否存在、读文档与 CLI 形态，**不要**真实出片。
- **成败判定**：小云雀侧已出片（网页创作历史有记录）即为**成功**，哪怕我方 API 返回 `fetch failed` 或没拿到下载链。我方超时或镜像 GCS 失败时，必须回报 `thread_id` 或网页会话链接，**禁止**只报失败让用户以为没生成而重打（双倍烧积分）。
