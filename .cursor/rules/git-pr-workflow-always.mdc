---
description: mvstudiopro — 改動須走 PR，勿直推 main
alwaysApply: true
---

# Git / PR 工作流（本倉庫）

- **完成代碼修改後**：在**功能分支**上 commit，**`git push` 該分支**，並用 **`gh pr create`**（或 GitHub 網頁）開 PR 對準 **`main`**；不要 **`git push origin main`** 把改動直接推到主線。
- **例外**（僅在用戶**明文**說「直推 main / 緊急熱修 / 不要 PR」時）：才可直推 `main`，並在回覆裡說明原因。
- PR 描述用簡短完整句：改了什麼、為什麼；必要時附關聯 issue / run 連結。
- **开 PR 节流（硬）**：**两次 `gh pr create` 之间至少间隔 15–20 分钟**（以墙上时钟为准，取 **≥15 分钟**）。不论代码写到哪、模块是否「感觉该切」、后台脚本是否催——**不到间隔禁止开下一张 PR**。禁止两分钟连开（#788–#792 即此错）。
- **模块优先**：尽量一个完整模块开一张 PR；节流是下限，不是鼓励到点就开空/碎 PR。
- **串行合併門禁**：開出 PR 後，必須監測到該 PR **MERGED**，才可 **開下一個** PR。禁止上一個還開著就連開下一個。
- **Deploy 門禁（硬）**：上一 PR 的 `main` Fly Deploy 必須是 **success**，才可 **`gh pr merge` 下一 PR**。`cancelled` / `failure` / `in_progress` 都不算过。禁止「刚 MERGED 十几秒就合下一刀」——会把上一刀 Deploy **顶掉取消**（#790 被 #791 挤掉即此错）。
- **合併 ≠ 部署成功**：`gh pr view` 顯示 MERGED 不夠；還要查对应 Actions 是否 **success**。若 Deploy **failure** / **cancelled**，视同未完成：等下一次含该提交的 Deploy 绿，或重跑/热修，直到绿。
- **合併期間不停工**：可繼續在功能分支**寫代碼**；**开 PR** 受 15–20 分钟节流 + Deploy 门禁约束。禁止「盯 Deploy → 立刻开合下一 PR」的自动连合脚本。
- **自动化时可代合**：用户说「全面自动化 / 自己 merge」时，须同时满足：CI 过、**上一 Deploy 已绿**、**距上一张 PR 创建 ≥15 分钟**。仅用户明文「我来合 / 不要代合」时才停手。
- **合并/部署失败自修**：无法 merge / CI 红 / Deploy failure·cancelled → 自己查原因、改、再推，直到 Deploy success；不要甩给用户（除非缺密钥/权限）。
