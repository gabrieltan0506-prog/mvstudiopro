---
description: mvstudiopro — 改動須走 PR，勿直推 main
alwaysApply: true
---

# Git / PR 工作流（本倉庫）

> **覆盖关系**：用户明文指令优先于下列门禁。详见 [`user-instruction-priority-always.mdc`](./user-instruction-priority-always.mdc)。

- **完成一小段代碼後**：在**功能分支**上 commit 並立刻 **`git push`**（遠端備份、可繼續開發）；**不要**每推一次就開 PR。push / 開 PR 前先過 [`local-verify-before-push-always.mdc`](./local-verify-before-push-always.mdc)（`pnpm check` + 相關 vitest）。
- **開 PR 時機**：攒到**一整塊模塊**告一段落後，再用 **`gh pr create`**（或 GitHub 網頁）對準 **`main`**；不要 **`git push origin main`** 把改動直接推到主線。
- **例外**（僅在用戶**明文**說「直推 main / 緊急熱修 / 不要 PR」時）：才可直推 `main`，並在回覆裡說明原因。
- PR 描述用簡短完整句：改了什麼、為什麼；必要時附關聯 issue / run 連結。
- **开 PR 前检查（硬）**：`gh pr create` **之前**必须先查：当前是否仍有**未合并**的 PR（尤其自己开的上一张）。**上一张还 OPEN / 未 MERGED → 禁止开新 PR**；只许继续往**已有功能分支** `commit` + `push`（叠进同一 PR），或等用户合并。#812 叠 #811 前未查即属违规。
- **开 PR 节流（硬 · 用户 2026-07-29 改）**：**两次 `gh pr create` 之间至少间隔 15 分钟**（墙上时钟，**≥15 分钟**）。不到间隔禁止开下一张。禁止两分钟连开（#788–#792 即此错）。
- **开 PR 日上限（硬）**：**同一自然日（本地时区）`gh pr create` 不得超过 20 次**。到 20 即停：只允许继续写代码 / push 功能分支，**禁止**再开新 PR，直至次日或用户明文提高当日额度。与「模块整块再开 / 未合并不开」叠加，取更严者。
- **模块优先**：一个完整模块开一张 PR；节流是下限，不是鼓励到点就开空/碎 PR。
- **串行合併門禁**：開出 PR 後，必須監測到該 PR **MERGED**，才可 **開下一個** PR。禁止上一個還開著就連開下一個。
- **Deploy 門禁（硬）**：上一 PR 的 `main` Fly Deploy 必須是 **success**，下一張才適合合併。`cancelled` / `failure` / `in_progress` 都不算过；刚 MERGED 十几秒就合下一刀会把上一刀 Deploy **顶掉取消**（#790 被 #791 挤掉即此错）。**合併由 Agent 代合**（见「默认代合」）；Agent 须**主动报告** Deploy 是否已绿、现在合下一张是否安全。
- **合併 ≠ 部署成功**：`gh pr view` 顯示 MERGED 不夠；還要查对应 Actions 是否 **success**。若 Deploy **failure** / **cancelled**，视同未完成：等下一次含该提交的 Deploy 绿，或重跑/热修，直到绿。
- **部署門禁只認 Fly**：Vercel 已 Disconnect / Git 自動部署關閉。PR 上若仍有舊 Vercel check 紅燈可忽略；**不以 Vercel 為 merge/deploy 門禁**。
- **合併期間不停工**：可繼續在功能分支**寫代碼**并 **push 到已有 PR 的分支**；**开新 PR** 受「上一张已 MERGED」+「≥15 分钟」+ 日上限约束。禁止「盯 Deploy → 立刻开下一 PR」。
- **默认代合（用户 2026-07-29 明文授权 · 用户命令优先于本 rules）**：Agent **可以**执行 `gh pr merge`。口径为「**没说停就代合**」——开完 PR 且本地验证（`pnpm check`+相关 vitest）与 CI 通过后，直接 `gh pr merge --squash` 合入 `main`，无需等用户点名 PR 号。
  - **说停才停**：用户明文说「停 / 别合 / 先等我 / 这张我自己合」时，立即停在开 PR，不代合。
  - 合前仍应确认：本地验证绿 + 该 PR 无阻断性 CI 红（Vercel 旧红灯可忽略，见下）。
  - 合后**主动报告**：PR 号、合入结果、随后盯 `main` Fly Deploy 是否 success。
  - 撤销记录：2026-07-26「禁止代合」条已被用户 2026-07-29「我的命令优先于 agent rules / 没说停就直接合并」明文覆盖作废。
- **串行代合仍守（软）**：合完上一张、盯到其 `main` Fly Deploy **success**，再合下一张；避免刚 MERGED 十几秒就合下一刀把上一刀 Deploy 顶掉取消（#790 被 #791 挤掉之错）。用户催得急时可并行，但要在回复点明风险。
- **部署失败自修**：CI 红 / Deploy failure·cancelled → 自己查原因、改、再推、再合，直到 Deploy success；不要甩给用户（除非缺密钥/权限）。
