---
description: mvstudiopro — 改動須走 PR，勿直推 main
alwaysApply: true
---

# Git / PR 工作流（本倉庫）

- **完成代碼修改後**：在**功能分支**上 commit，**`git push` 該分支**，並用 **`gh pr create`**（或 GitHub 網頁）開 PR 對準 **`main`**；不要 **`git push origin main`** 把改動直接推到主線。
- **例外**（僅在用戶**明文**說「直推 main / 緊急熱修 / 不要 PR」時）：才可直推 `main`，並在回覆裡說明原因。
- PR 描述用簡短完整句：改了什麼、為什麼；必要時附關聯 issue / run 連結。
- **串行合併門禁**：開出 PR 後，必須監測到該 PR **MERGED**，才可 **push / 開下一個** PR。禁止上一個還開著就連開下一個（部署互相取消）。
- **合併 ≠ 部署成功**：`gh pr view` 顯示 MERGED 不夠；還要查 `main` 上對應 Actions（如 Deploy app）是否 **success**。若 Deploy **failure**，視同未完成，立刻查 log 自修再開熱修 PR。
- **合併期間不停工**：PR 合併/部署時繼續在下一功能分支寫代碼；約 **每 10 分鐘**檢查上一 PR 是否已合**且 Deploy 綠**；綠了再 push 下一 PR。
- **自動化時可代合**：用戶說「全面自動化 / 自己 merge / 你來合」時，CI 過或 mergeable 後**自行 `gh pr merge`**；不要干等用戶點合併。僅用戶明文「我來合 / 不要代合」時才停手。
- **合並/部署失敗自修**：若無法 merge / CI 紅燈 / Deploy failure / 衝突 → **自己查原因、本地改、再 push**，直到 Deploy success；不要把「請你手動處理」丟回用戶（除非缺密鑰/權限等必須用戶介入）。
