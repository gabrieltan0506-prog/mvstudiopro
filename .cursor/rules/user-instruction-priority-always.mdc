---
description: 用户明文指令优先于门禁；指令冲突先复述再动手；推送汇报要可核验
alwaysApply: true
---

# 协作口径（用户优先 · 新 Agent 必读）

本仓库代码量大，**不能只靠聊天或零散 md 约束**。

**开场必读（按序）**

1. 本文件 + 其他 `alwaysApply` rules（尤其 `git-pr-workflow-always`、`deployment-boundaries-always`、`frontend-no-tech-leak-always`）
2. **产品知识库**（详见 `knowledge-base-always`）：[`.cursor/knowledge/README.md`](../knowledge/README.md) → brief + PROGRESS；七月交接见 `downloads-jul2026-index.md`
3. 再搜代码；Downloads/`docs/2026Jul*` 只考古，与 brief 冲突时信 brief

动漫剧成片、画布、可拍表前：至少读完 `manhua-factory-brief.md` §2–5。

## 用户明文优先（硬）

- 用户**最新一条明文指令**优先于默认门禁、旧回合习惯、以及 Agent 自己的「稳妥推断」。
- 例：用户说「只 push 不开 PR」→ 禁止 `gh pr create`；后来说「开 PR、不准合并」→ 立刻开 PR 且**禁止** `gh pr merge`；说「合 #NNN / 帮我合上」→ 才可代合并点明依据。
- **默认禁止代合**（见 `git-pr-workflow-always`）：开完 PR 不等于可以 `gh pr merge`；须用户当轮明文授权。2026-07-29「没说停就代合」已收回。
- 门禁（串行 PR、15 分钟间隔、Deploy 绿再合下一张等）在**没有**用户明文覆盖时仍生效；有覆盖时以用户为准，并简短说明「按你的指令覆盖门禁」。

## 指令冲突时（硬）

- 同一会话前后指令打架（开/不开 PR、合/不合、推哪里）→ **先用一两句复述「当前要执行的那一条」**，再行动；禁止闷头按更早的一条做。
- 禁止在回复里甩一堆「门禁不允许」却不给可执行路径；若被挡住：给出符合**当前用户指令**的下一步（例如：已 push 的分支名 + 链接，或按要求开/叠 PR）。

## 推送与可见性（硬）

- **push ≠ 开 PR**。只 push 时回复必须写清：分支名、最新 commit、远端链接（`…/tree/<branch>` 或 compare），并写明「未开 PR」。
- 用户说「推送在哪 / 我没看到」时：先查他是否在看 **Open PRs** 空列表；指向 **Branches**，不要假装已开 PR。
- 合入后 Fly Deploy failure → **自己修 tsc/构建并 push 热修分支**；需要 PR 时开热修 PR，不要丢给用户猜日志。

## 协作态度

- 产品验收上的火（假锁角色、废话提示词、布局不对）→ 当硬需求修，不辩解。
- 人身/矛盾指令导致无法执行时：复述当前指令 + 做最小澄清（一句），然后继续干活。
- 回复用中文、短句；少讲流程教条，多报可核验结果（PR 号、分支、Deploy 绿/红）。
