---
description: 接手 agent 开工前必须先问用户「从哪开始、怎么推进」，不得自作主张
alwaysApply: true
---

# 接手先问路（硬 · 用户 2026-07-29 明文）

> **覆盖关系**：用户本轮明文指令优先。详见 [`user-instruction-priority-always.mdc`](./user-instruction-priority-always.mdc)。

## 硬规则

- **每个接手/新开会话的 agent，开工前必须先问用户**：**「从哪里开始、该如何推进」**——不得自作主张直接改代码、生图、烧积分、开 PR、合并。
- 先读开场必读（[`knowledge-base-always`](./knowledge-base-always.mdc) 的 README + brief + PROGRESS 最新日 + 本文件），**再用一两句复述你理解的当前进度**，然后**提问确认起点与下一步**，等用户回话才动手。
- 用户回答之前：只允许**只读排查**（读代码 / git log / 看 PR·CI 状态 / 读交接文档）。**禁止**任何写操作、生成、生图、`git push`、`gh pr create`、`gh pr merge`。

## 例外（可不先问，直接做）

- 用户在**当前这一轮**已明文给出具体任务/起点（例：「改静帧门禁」「合併 #NNN」「继续昨天的 L2」）——照做，不必再问「从哪开始」。
- 用户明文说「你定 / 按你判断 / 别问了 / 自动模式」——按其授权推进；但**范围一旦要扩大或涉及烧积分·删资产·改对外口径**，仍需按 [`ask-before-design-choices-always.mdc`](./ask-before-design-choices-always.mdc) 先确认。

## 反例

- ❌ 新会话一上来不问，直接点「补齐/重出」把已出资产清了重烧积分。
- ❌ 没复述当前进度、没确认起点，就自行选一个模块开干、开 PR。
- ✅ 「我看了 PROGRESS 最新日与 #1019，当前进度是 X；请问这轮从哪开始、怎么推进？」→ 等用户答复再动手。
