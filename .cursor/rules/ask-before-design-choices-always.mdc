---
description: 遇产品/代码设计选择题时，先提问或给选项，由用户拍板后再实现
alwaysApply: true
---

# 设计选择题：先问用户再写代码

当出现**多种合理实现路径**、且选择会影响产品语义、文案口径、审核边界、数据结构或用户可见行为时，**禁止擅自拍板后直接改代码**。

## 必须先停下来的情况（包括但不限于）

- 产品口径分歧（例如：致敬导演 vs 只借手法不点名）
- 文案/Prompt 软边界 vs 硬禁令的力度
- 库/名单扩容范围（加谁、电影 vs 电视剧、同名歧义）
- 字段/表结构/六栏分镜等用户可见格式变更
- 默认示例、人设偏置、多样性策略的取舍
- 两套以上架构或 API 形状都能工作

## 正确做法

1. **用简短中文**说明分歧点（1–3 句）。
2. **给出 2–4 个选项**（A/B/C…），每项写清：做什么、不做什么、对产出的影响。
3. 可附**推荐项**并说明理由，但**等用户明确选择**后再改代码 / push / PR。
4. 若信息不足，先**提问**补齐（例如同名导演指哪一位），不要猜完就写死。

## 例外（可直接做）

- 用户已明文选定方案，或说「按你推荐做 / 你定」
- 纯机械修复（typo、tsc、明显 bug、与既有规则一致的小补丁）
- 用户要求「先实现一版再调」且范围已框死

## 反例

- ❌ 用户只澄清「有的是电视剧导演」→ 直接扩库并改完全部 Prompt
- ❌ 用户说「只借手法不致敬」→ 未确认对外文案是否完全去导演名，就大改多文件
- ✅ 列出选项：「A 内部溯源+对外去名 / B 对外可写手法标签但不写人名 / C …」→ 等回复再改
